# Security Policy

**Found a vulnerability? Go straight to [Reporting a Vulnerability](#reporting-a-vulnerability).**

Everything else here documents what the project already enforces:
[Supported Versions](#supported-versions) · [Verifying Release Authenticity](#verifying-release-authenticity) · [Security Documentation](#security-documentation) · [GDPR Cross-Reference](#gdpr-cross-reference) · [Test Enforcement Matrix](#test-enforcement-matrix)

## Supported Versions

Security fixes are provided for the `main` branch only, and reach operators through the next
release: the support policy is rolling — a fix always lands ahead, never as a backport to an
existing tag, and there are no maintenance branches. An operator pinned to a release tag picks up
a security fix by updating to the newest tag.

| Version | Supported |
| --- | --- |
| `main` | :white_check_mark: |
| newest release tag | :white_check_mark: — supported as the delivery vehicle; a fix arrives as the *next* tag, never as a backport |
| older tags | :x: — update to the newest tag |

## Verifying Release Authenticity

Published container images are keyless-signed with [cosign](https://github.com/sigstore/cosign)
and carry a [SLSA build provenance](https://slsa.dev) attestation and a software bill of
materials, all produced by this repository's GitHub Actions and pushed to the registry. Before
running an image you can confirm it was built by this repository's CI from this source, rather
than substituted or tampered with.

The tag is written last. The release workflow pushes the image by digest under no tag at all, signs
and attests that digest, reads both back from the registry, and only then points `vX.Y.Z` and
`latest` at it — so a tag that resolves at all resolves to a signed digest. A failure before the tags
are written leaves none of them; one during the write can leave the tags written before it, and
those resolve to the signed digest like any other. There is no failure that produces an unsigned
one.

Verify the signature (substitute the tag you are pulling for `vX.Y.Z`):

```bash
cosign verify ghcr.io/ovumcy/ovumcy-web:vX.Y.Z \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  --certificate-identity-regexp '^https://github\.com/ovumcy/ovumcy-web/\.github/workflows/docker-image\.yml@'
```

Verify the build provenance with the GitHub CLI:

```bash
gh attestation verify oci://ghcr.io/ovumcy/ovumcy-web:vX.Y.Z --repo ovumcy/ovumcy-web
```

A failed check means the image was not produced by this repository's release workflow. Do not run it.

## Reporting a Vulnerability

Please report security vulnerabilities privately — not through public GitHub
issues. Use either channel:

- GitHub private vulnerability reporting (preferred):
  https://github.com/ovumcy/ovumcy-web/security/advisories/new
- Email: contact@ovumcy.com (subject: `SECURITY: <short summary>`)

Include impact, reproduction steps, affected endpoints/files, and a suggested
fix if you have one.

We practice coordinated vulnerability disclosure. After a report we acknowledge
receipt within 72 hours, share a triage assessment and remediation plan within
7 days, and aim to ship a fix and coordinate public disclosure within 90 days —
sooner for actively exploited issues. Please give us reasonable time to
remediate before disclosing a vulnerability publicly.

## Security Documentation

Detailed security design lives in per-concern documents under [`docs/security/`](docs/security/). This policy keeps the operator-facing essentials above plus the GDPR cross-reference and the Test Enforcement Matrix (the source-of-truth mapping of each claim to its Go test).

- [Known Information Disclosure](docs/security/known-disclosures.md) — accepted residual signals (register-enumeration oracle, the login `requires_totp` 2FA-status response, absolute session expiry, process-local limiter state, address-keyed login budget, unaudited calendar-feed polling, password reset issuing a session without a second-factor challenge, no duress mode and sequential account ids).
- [OIDC Account Linking & Session Invalidation](docs/security/oidc-and-sessions.md) — upstream-IdP trust boundary and credential-rotation session revocation.
- [Cryptography: Field Encryption, Cookies & SECRET_KEY](docs/security/cryptography.md) — field-level encryption, the cookie inventory, and the SECRET_KEY usage map.
- [Data Inventory, Retention & Deletion](docs/security/data-handling.md) — what is persisted per account and how clear-data / delete-account erase it.
- [Password & Auth Policy, Rate Limits](docs/security/auth-policy-and-rate-limits.md) — password/recovery/TOTP policy and the per-IP / per-account rate limits.
- [Threat Model](docs/security/threat-model.md) — in-scope defences and explicit out-of-scope assumptions.
- [Logging Policy](docs/security/logging.md) — the off-by-default audit stream and request-log sanitization.

## GDPR Cross-Reference

Ovumcy is privacy-critical software handling special-category personal data (Art. 9). Operators of self-hosted instances act as data controllers; Ovumcy provides the technical controls and operators provide deployment-specific policy. The full operator-facing compliance walkthrough lives in [`docs/gdpr.md`](docs/gdpr.md). The table below maps each in-scope obligation onto the existing technical control and its enforcing test.

| GDPR Article | Obligation | Technical control | Enforcing test |
| --- | --- | --- | --- |
| Art. 5(1)(c) — Data minimisation | Only data necessary for the cycle-tracking purpose is stored | `users`, `daily_logs`, `symptom_types` schemas as described in [*Data Inventory*](docs/security/data-handling.md#data-inventory); no analytics, telemetry, or third-party identifiers | `internal/db/migrations.go` + schema in `migrations/` |
| Art. 5(1)(f) — Integrity & confidentiality | Auth/recovery/reset cookies sealed; TOTP secret encrypted at rest; CSRF on every mutation | AES-256-GCM + HKDF-derived keys + AAD binding | `internal/api/secure_cookie_codec_*_test.go`, `internal/security/field_crypto_test.go`, `internal/api/state_mutation_csrf_regression_test.go` |
| Art. 9(2)(a) — Explicit consent | Registration captures explicit consent in the application itself; two paths can carry health-derived data off the instance, both off until the owner arms them, both scoped to that owner's own account, and both revocable | `POST /api/v1/users` refuses a request whose `consent` field is absent or falsy with `auth.error.consent_required` (the checkbox lives on `/register`); `/privacy` renders the in-app privacy notice; there is no analytics, telemetry or third-party identifier. The standing egress paths to a third party are exactly two (the session-gated export downloads are the subject of the Art. 15/20 rows below). **Webhook reminders**: delivered to the destination that owner configured, under the request envelope in [*Webhook Notifications*](#webhook-notifications-outbound-egress), cleared by clear-data. **The read-only `.ics` calendar feed**: a capability-token URL the owner generates and hands to a calendar client — commonly a third-party service such as a hosted calendar, which then receives that owner's predicted period and fertile-window days as a processor of the operator's choosing. It is read-only, owner-scoped, hashed at rest, revealed once, per-IP rate-limited, revocable by rotation and force-cleared with any credential rotation ([*Calendar Feed Subscription*](#calendar-feed-subscription-ics)); its polling is deliberately not audited, an accepted residual recorded in [*Known Information Disclosure*](docs/security/known-disclosures.md) | `TestRegisterRejectsMissingConsent`, `TestRegisterRejectsExplicitlyFalseConsent` in [internal/api/auth_register_consent_regression_test.go](internal/api/auth_register_consent_regression_test.go); `TestWebhookDeliverySucceedsOn2xx`, `TestWebhookDeliveryLogsHostOnly` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go); `TestResolveFeedReturnsOwnersFeedForValidToken`, `TestResolveFeedCrossUserIsolation` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go); `internal/api/privacy_route_regressions_test.go`, `e2e/privacy.spec.ts` |
| Art. 13/14 — Information to subject | `/privacy` page enumerates storage location, scope of processing, and opt-out paths in six locales | `internal/templates/privacy.html` + `internal/i18n/locales/*.json` privacy keys | `internal/api/privacy_route_regressions_test.go`, `e2e/privacy.spec.ts` |
| Art. 15 — Right to access | Subjects export the day-level health record as CSV or JSON — the scope, and what falls outside it (the account profile, cycle/tracking settings, reminder/webhook configuration, and symptoms that were only ever archived and never logged), is stated in [`docs/gdpr.md`](docs/gdpr.md) | `GET /api/v1/exports/csv`, `GET /api/v1/exports/json`, `GET /api/v1/exports/summary` | `internal/api/export_regressions_test.go` and siblings |
| Art. 16 — Right to rectification | Day entries, symptoms, the display name, and the cycle, tracking, reminder and interface settings are editable from the dashboard, calendar, and settings UI; the email address is read-only after registration (Art. 34 row below), and credentials rotate through their own flows rather than being edited | `PUT /api/v1/days/{date}`, `PATCH /api/v1/users/current/profile`, `PATCH /api/v1/symptoms/{id}`, and the sibling settings `PATCH` endpoints | `internal/api/day_upsert_canonicalization_regression_test.go`, `internal/api/settings_profile_persist_regressions_test.go`, `internal/api/settings_symptoms_*_regression_test.go` |
| Art. 17 — Right to erasure | `clear-data` removes health records; `delete-account` explicitly erases every user-scoped row (daily logs, symptoms, OIDC identities, register-pickup tokens, OIDC logout states) plus the account | `POST /api/v1/users/current/data-wipe`, `DELETE /api/v1/users/current` | `internal/api/settings_clear_data_flow_test.go`, `internal/api/settings_delete_account_regression_test.go`, `internal/db/delete_account_completeness_test.go`, `TestClearDataPreservesAccountIdentityFields` |
| Art. 20 — Right to portability | Export endpoints return machine-readable formats and the JSON export can be re-imported into a fresh instance (additive restore — never overwrites or deletes existing days); structure documented in `docs/export.md` | `GET /api/v1/exports/csv`, `GET /api/v1/exports/json`, `POST /api/v1/imports/json` | `internal/api/export_regressions_test.go`, `internal/services/export_service_integration_test.go`, `internal/api/imports_regressions_test.go`, `internal/services/import_service_test.go` |
| Art. 22 — Automated decision-making | Cycle predictions are statistical aggregations of the subject's own logs; no decision producing legal effects | Prediction policy in `internal/services/prediction_explanation.go`; copy explains conservative bounds | `internal/services/prediction_explanation_test.go`, `internal/services/calendar_view_service_test.go` |
| Art. 25 — Privacy by design / by default | Owner-only middleware on every mutation; auto-period-fill **off by default**; audit log **off by default** | `handler.OwnerOnly` chained explicitly on every `/api/v1/*` mutation behind `AuthRequired` — the exclusion set is exactly the pre-session endpoints (register, login, the 2FA challenge, the password-reset pair), named in [docs/SECURITY_INVARIANTS.md](docs/SECURITY_INVARIANTS.md) and nowhere else in prose; `models.DefaultAutoPeriodFill` is the single value both account constructors, the gorm column default and the `clear-data` reset read, so a new — or freshly wiped — account writes no inferred period days until its owner turns auto-fill on, while an account already using it keeps it (no migration rewrites a stored value); `AUDIT_LOG_ENABLED=false` startup default | `TestEveryAuthenticatedV1MutationChainsOwnerOnly` in [cmd/ovumcy/owner_only_route_chain_guard_test.go](cmd/ovumcy/owner_only_route_chain_guard_test.go), `TestUnsupportedRoleRejectedAcrossEveryAuthedV1Route` and `TestPreSessionV1MutationExclusionsMatchTheSecurityInvariantsDocument` in [internal/api/owner_only_coverage_regression_test.go](internal/api/owner_only_coverage_regression_test.go); `TestOwnerAccountConstructorsLeaveAutoPeriodFillOff` in [internal/services/auth_service_account_defaults_test.go](internal/services/auth_service_account_defaults_test.go); `TestNewAccountsCarryAutoPeriodFillOff`, `TestClearAllDataTurnsAutoPeriodFillOff` in [internal/db/user_auto_period_fill_default_test.go](internal/db/user_auto_period_fill_default_test.go); `internal/api/security_event_logging_audit_flag_test.go`, `cmd/ovumcy/main_test.go` (`TestLoadRuntimeConfigDefaultsAuditLogOff`) |
| Art. 30 — Records of processing | Per-action audit stream available under `AUDIT_LOG_ENABLED=true`; sanitized to drop PII | `internal/api/security_event_logging.go`, `SafeRequestLogPath` | `internal/api/security_event_logging_audit_flag_test.go`, `internal/api/security_event_logging_mutation_regression_test.go` |
| Art. 32 — Security of processing | Sealed cookies, encrypted TOTP secrets, owner-only access, rate limits, CSP, secure headers; operator adds disk-level encryption | Multiple — see the [*Cryptography*](docs/security/cryptography.md) and [*Rate Limits*](docs/security/auth-policy-and-rate-limits.md#rate-limits) concern docs; `cmd/ovumcy/server.go` security headers | `internal/api/secure_cookie_codec_*_test.go`, `internal/security/field_crypto_test.go`, `cmd/ovumcy/main_test.go` (security headers, rate-limit PII), `internal/api/owner_only_coverage_regression_test.go` |
| Art. 33 — Breach notification (72 h) | Disclosure channel published; operator-side runbook in `docs/gdpr.md` | [`SECURITY.md → Reporting a Vulnerability`](#reporting-a-vulnerability), [`docs/gdpr.md → Breach Notification`](docs/gdpr.md#breach-notification-art-33) | Policy-level — not test-enforceable |
| Art. 34 — Communication to subject | Operator obligation; Ovumcy provides per-account email addresses for outreach | Stored in `users.email` (read-only after registration via UI) | Not test-enforceable |
| Art. 35 — DPIA | Operator obligation; Ovumcy ships the technical inputs (data inventory, threat model, encryption posture) needed to build one | [*Data Inventory*](docs/security/data-handling.md#data-inventory), [*Threat Model*](docs/security/threat-model.md#threat-model), [`docs/gdpr.md`](docs/gdpr.md) | Policy-level — not test-enforceable |

Items marked *Policy-level* are operator obligations that cannot be verified by `go test`. Items marked with concrete test files are verified by the runtime test suite; their concrete claim mapping lives in [*Test Enforcement Matrix*](#test-enforcement-matrix) below.

## Test Enforcement Matrix

This section maps each test-enforceable claim in this policy (and the concern documents under `docs/security/`) to the Go test that guards it. It is the mechanical check that the privacy and security claims remain true. When a claim changes, the corresponding test must change too; when a test is removed, the claim is no longer enforced and must be retracted.

Policy-level claims (threat model in/out-of-scope, design rationale, marketing-style statements like "privacy-first") are intentionally excluded — they are reviewed by humans, not by `go test`.

### Layering

| Claim | Enforced by |
| --- | --- |
| `templateToJSON` returns a plain string (not `template.JS`/`template.HTML`), so owner- or attacker-controlled values embedded in a `data-*` attribute (`data-chart`, `data-supported-languages`) stay subject to `html/template`'s contextual attribute escaping — HTML/JS-breaking characters cannot break out of the attribute or inject markup | `TestTemplateToJSONEscapesAttributeContext` in [internal/api/template_tojson_attr_escaping_test.go](internal/api/template_tojson_attr_escaping_test.go) |
| A caller-supplied query parameter is never echoed back into a rendered page or into an outgoing URL. The address the shared layout renders on every page (the language switcher's `next` field, the footer `/privacy?back=…` link) and the privacy page's own back link keep only the parameters the page handlers read — `month`, `day`, `selected`, `edit`, `step`, `back` — **and only where the value also matches the shape its own consumer accepts**, since a name-only allowlist renders `?step=victim@example.com` exactly as it would `?email=`. The fragment is dropped, the surviving parameters are emitted in a deterministic order, `back` is filtered by the same policy one level down and must name a path this app's routes can spell, and a query the parser rejects yields the bare path. So a crafted link such as `/login?email=…` cannot plant an address in the page's markup, in browser history, or in the `/privacy` access log | `TestSanitizeCurrentPathQuery` in [internal/services/redirect_policy_current_path_query_test.go](internal/services/redirect_policy_current_path_query_test.go), `TestBuildPrivacyBackNavigationFiltersTheQueryOfTheBackPath` in [internal/services/privacy_page_policy_test.go](internal/services/privacy_page_policy_test.go); the rendered surfaces in `TestPublicPageDropsUnlistedQueryParametersFromTheRenderedPath`, `TestPrivacyPageFiltersTheQuerySmuggledInsideItsBackParameter`, `TestSignedInPageKeepsItsAllowlistedQueryParameters` ([internal/api/current_path_query_leak_regression_test.go](internal/api/current_path_query_leak_regression_test.go)) |

### Field-Level Encryption

| Claim | Enforced by |
| --- | --- |
| `users.totp_secret` is AES-256-GCM encrypted under a key derived from `SECRET_KEY` via HKDF-SHA256 | `TestEncryptDecryptField_RoundTrip` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) proves the round trip; the construction itself — HKDF salt/info labels, derived key, `nonce‖ciphertext` layout, base64url framing — is pinned by the fixed-key golden ciphertexts in `TestDecryptFieldOpensPreConsolidationGoldenCiphertexts` ([internal/security/field_crypto_golden_test.go](internal/security/field_crypto_golden_test.go)), which a round trip alone cannot do (it would pass under any AEAD) |
| AAD-bound to row id; cross-row substitution under a different AAD fails to open | `TestDecryptField_RejectsWrongAAD` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Ciphertext is non-deterministic (distinct outputs for the same plaintext) | `TestEncryptField_ProducesDistinctCiphertexts` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Wrong key fails to decrypt | `TestDecryptField_WrongKey` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Tampered ciphertext fails to decrypt | `TestDecryptField_TamperedCiphertext` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Legacy no-AAD ciphertexts open through the fallback path | `TestDecryptField_LegacyFallback` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Empty `SECRET_KEY` is refused | `TestEncryptField_EmptyKey`, `TestDecryptField_EmptyKey` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Golden pre-consolidation ciphertexts (AAD-bound and legacy no-AAD) still decrypt to the same plaintext | `TestDecryptFieldOpensPreConsolidationGoldenCiphertexts` in [internal/security/field_crypto_golden_test.go](internal/security/field_crypto_golden_test.go) |

### OIDC Account Linking

| Claim | Enforced by |
| --- | --- |
| The public `/auth/oidc/link-confirm` route stays closed for good (issue #701): the callback never mints the pending-link cookie either handler reads, for any target account, so posting a password there can never complete a link | `TestOIDCCallbackPendingLinkNeverMintsPendingCookieAndRedirectsToLogin`, `TestPublicOIDCLinkConfirmRouteNeverCompletesALinkWithoutAMintedCookie` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| Linking a new OIDC identity from an authenticated session requires the Settings step-up's live session AND a fresh provider re-authentication — refused with no session, refused with the step-up cookie missing or its freshness proof stale | `TestOIDCIdentityLinkStepupStartRefusesWithoutALiveSession`, `TestOIDCIdentityLinkStepupCallbackRefusesWithoutTheStepupFactor`, `TestOIDCIdentityLinkStepupCallbackRefusesAStaleReauth` in [internal/api/settings_oidc_identity_link_test.go](internal/api/settings_oidc_identity_link_test.go) |
| A step-up cookie minted for one owner cannot link an identity onto another owner's session | `TestOIDCIdentityLinkStepupCallbackRefusesAForeignSession` in [internal/api/settings_oidc_identity_link_test.go](internal/api/settings_oidc_identity_link_test.go) |
| A successful Settings step-up calls `ConfirmAndLinkIdentity` for the same account that started the flow, with the exchange's own `(issuer, subject)` — the identical binding the old public route created | `TestOIDCIdentityLinkStepupCompletesAndCreatesTheBinding` in [internal/api/settings_oidc_identity_link_test.go](internal/api/settings_oidc_identity_link_test.go), `TestOIDCLoginServiceCompleteIdentityLinkReauthLinksOnFreshExchange` in [internal/services/oidc_identity_link_reauth_test.go](internal/services/oidc_identity_link_reauth_test.go) |
| `CompleteIdentityLinkReauth` refuses a stale freshness proof and a cross-user `(issuer, subject)` claim, without ever persisting a link | `TestOIDCLoginServiceCompleteIdentityLinkReauthRefusesStaleExchange`, `TestOIDCLoginServiceCompleteIdentityLinkReauthRefusesCrossUserClaim` in [internal/services/oidc_identity_link_reauth_test.go](internal/services/oidc_identity_link_reauth_test.go) |
| The no-session recovery path (`ovumcy link-oidc-identity`) addresses an account by `--id` exactly as `reset-password` does, links via the same `ConfirmAndLinkIdentity`, and refuses an unknown id without persisting anything | `TestRunLinkOIDCIdentityCommandLinksByID`, `TestRunLinkOIDCIdentityCommandRefusesUnknownID` in [internal/cli/link_oidc_identity_test.go](internal/cli/link_oidc_identity_test.go) |
| A later OIDC sign-in through an already-linked identity is gated on the account's second factor the same way local login is: `OIDCLoginService.Authenticate` derives `RequiresPasswordReset` (true when `MustChangePassword` is set OR TOTP is enrolled but unverifiable) and sets `RequiresTOTP` only when neither holds; the handler routes to `/reset-password` or `/auth/2fa` before ever calling `setAuthCookie` | `TestOIDCCallbackForLinkedTOTPAccountGatesOnTheSecondFactor`, `TestOIDCCallbackRoutesRequiresPasswordResetToResetEvenWithoutMustChangePassword` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) / [internal/api/auth_totp_unverifiable_secret_test.go](internal/api/auth_totp_unverifiable_secret_test.go); `TestOIDCLoginServiceAuthenticateSetsRequiresTOTPForLinkedTOTPAccount`, `TestOIDCLoginServiceAuthenticateMustChangePasswordOutranksRequiresTOTP`, `TestOIDCLoginServiceAuthenticateRoutesUnverifiableTOTPToForcedReset` in [internal/services/oidc_login_service_test.go](internal/services/oidc_login_service_test.go) |
| The still-registered public `POST /auth/oidc/link-confirm` is CSRF-protected at the middleware layer — ahead of the handler, so a request without `csrf_token` is refused `403` whatever cookie it carries | `TestCompleteOIDCLinkConfirmationRejectsRequestWithoutCSRFToken` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| That handler's first statement — reached before any cookie is read — is the instance-wide local-sign-in gate: with local sign-in switched off, a correct password is refused with Login's own refusal spec and `ConfirmAndLinkIdentity` never runs | `TestCompleteOIDCLinkConfirmationRefusesEntirelyWhenLocalSignInDisabled` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |

The rows above are what a user can reach today: the Settings step-up, the operator CLI, and the
two gates that still run on the public `/auth/oidc/link-confirm` route. **That route is still
registered and still serving** — issue #701 closed the linking flow, it did not unregister the
endpoint, so both methods answer any unauthenticated visitor: the POST clears CSRF validation in
the middleware and the instance-wide local-sign-in check in the handler *before* any cookie is
read, and only then redirects to `/login` because the pending-link cookie is absent. Treat it as
live public attack surface when scoping one.

What issue #701 retired is everything from the pending-link cookie read onward: the OIDC callback
mints that cookie for no account in any configuration, so the read succeeds for nobody and the
code behind it cannot run. The rows below are that half. Their tests still run and still pass, so
each claim stays true **about the handler's code** while describing no flow a user can complete;
they are kept rather than deleted because they are the barrier again the moment anything is
allowed to mint that cookie.

| Claim (`/auth/oidc/link-confirm`, from the pending-link cookie read on: true of the code; since issue #701 that read succeeds for nobody) | Enforced by |
| --- | --- |
| Pending-link cookie is rejected on tamper / cross-purpose AAD / rotated key / payload expiry | `oidc_link_pending_cookie_test.go` in `internal/api/` |
| Wrong password keeps the pending-link cookie alive for retry within the 5-minute TTL | `TestCompleteOIDCLinkConfirmationKeepsCookieOnWrongPassword` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| Accounts with `local_auth_enabled=false` (OIDC-only) are refused at the link-confirm path — the per-account column, re-read from the cookie's target user after the cookie read, not the instance-wide gate above | `TestCompleteOIDCLinkConfirmationWithLocalAuthDisabledRefusesUnavailable` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| TOTP-enabled targets require a valid 6-digit code together with the password; missing / wrong / replayed codes refuse the link and do not issue a session — replay is refused by the persistent per-user step floor, so a captured code is dead for the rest of its own 30-second window, not merely on the surface that consumed it | `TestCompleteOIDCLinkConfirmationWithTOTPEnabledRequiresValidCode`, `TestCompleteOIDCLinkConfirmationWithTOTPEnabledRefusesMissingCode`, `TestCompleteOIDCLinkConfirmationWithTOTPEnabledRefusesWrongCode`, `TestCompleteOIDCLinkConfirmationWithTOTPEnabledRefusesReplayedCode` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| Link-confirm form exposes the TOTP input only when the target account's TOTP is verifiable (enrolled AND the stored secret decrypts) — not the raw `totp_enabled` column | `TestShowOIDCLinkConfirmPageRendersTOTPFieldForTOTPEnabledTarget`, `TestShowOIDCLinkConfirmPageHidesTOTPFieldForNonTOTPTarget` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| `MustChangePassword` targets are routed to `/reset-password` instead of receiving an auth cookie | `TestCompleteOIDCLinkConfirmationRoutesMustChangePasswordToReset` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| A target enrolled in TOTP but with an unverifiable secret (SECRET_KEY rotation) is routed to `/reset-password` without ever demanding a `totp_code` — no code could ever satisfy it — and without issuing a session | `TestCompleteOIDCLinkConfirmationRoutesUnverifiableTOTPToResetWithoutASession` in [internal/api/auth_totp_unverifiable_secret_test.go](internal/api/auth_totp_unverifiable_secret_test.go) |
| `ConfirmAndLinkIdentity` provider/storage failures clear the pending cookie | `TestCompleteOIDCLinkConfirmationConfirmLinkErrorMappingClearsCookie` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| Successful link emits a sanitized `auth.oidc_link_confirm linked` security event | `TestCompleteOIDCLinkConfirmationEmitsAuditLogOnSuccess` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |

### Register Enumeration Residual

| Claim | Enforced by |
| --- | --- |
| `POST /api/v1/users` emits an identical-shape sealed pickup cookie for new and duplicate emails | `auth_register_regressions_test.go`, `auth_register_email_persistence_regressions_test.go` in `internal/api/` |
| Duplicate-email branch runs equalized bcrypt timing | `TestAuthenticateCredentialsEqualizesTimingForMissingUser`, `TestAuthenticateCredentialsEqualizesTimingForDisabledLocalAuth` in [internal/services/auth_service_credentials_timing_test.go](internal/services/auth_service_credentials_timing_test.go) |
| Timing-equalization placeholder hashes carry the production bcrypt cost (equalized paths are not measurably faster than a real compare) | `TestTimingEqualizationHashesMatchTargetCost` in [internal/services/auth_service_hash_cost_test.go](internal/services/auth_service_hash_cost_test.go) |
| Pickup nonce is single-use via `register_pickup_tokens` (atomic UPDATE) | `register_pickup_handler_test.go` in `internal/api/` |
| `GET /register/welcome` second consumption falls through to `/login` | `register_pickup_handler_test.go` in `internal/api/` |
| `GET /register/welcome` consumes the nonce and mints a session on a safe method the CSRF middleware never validates, so it is additionally refused for an off-origin initiator, for a destination no client of it has, and for a speculative load (Fetch Metadata). A cross-site navigation — the one case `SameSite=Lax` still sends the pickup cookie on — takes the same neutral `/login` exit as a decoy, leaving the nonce unspent for the owner's own arrival, as do an embed and a prefetch. Each check reads its own header and fires only on a value the browser stated, so the published `next_path` fetch, a partially stripped family, and a client sending none of the `Sec-Fetch-*` family are all admitted | `TestRegisterPickupRefusesAForeignRequestWithoutSpendingTheNonce`, `TestFirstPartyGuardAdmitsTheFetchOfThePublishedNextPath`, `TestFirstPartyGuardAdmitsAPartiallyStrippedFetchMetadataFamily`, `TestFirstPartyGuardAdmitsAClientThatSpeaksNoFetchMetadata`, `TestFirstPartyGuardedRoutesAreExactlyTheDeclaredSet` in [internal/api/first_party_guard_regressions_test.go](internal/api/first_party_guard_regressions_test.go) |
| Recovery code shape `OVUM-XXXX-XXXX-XXXX` | `TestValidateRecoveryCodeFormat`, `TestNormalizeRecoveryCode` in [internal/services/auth_input_policy_test.go](internal/services/auth_input_policy_test.go) |

### Cross-Owner Data Isolation (household self-hosting)

| Claim | Enforced by |
| --- | --- |
| An instance may host several independent owners; each owner's data is isolated by `user_id`, and a resource id from the request is always combined with the session user — `PATCH`/`DELETE`/`restore` of another owner's symptom returns 404 and leaves it unchanged | `TestUpdateSymptomByOtherUserReturnsNotFound`, `TestDeleteSymptomByOtherUserReturnsNotFound`, `TestRestoreSymptomByOtherUserReturnsNotFound` in [internal/api/symptoms_idor_regression_test.go](internal/api/symptoms_idor_regression_test.go) |
| The scoping holds while two owners are served **at the same time**: with twelve workers across two owner sessions writing and reading the same dates through one instance, every response carries only the requesting owner's rows, identity and cookies, and every persisted row keeps its own owner's marker | `TestOwnerIsolationHoldsUnderConcurrentCrossOwnerRequests` in [internal/api/owner_isolation_concurrency_regression_test.go](internal/api/owner_isolation_concurrency_regression_test.go) |
| A day upsert carrying another owner's `symptom_id` is rejected (400) and writes no log row for the requester | `TestUpsertDayRejectsSymptomIDOwnedByOtherUser` in [internal/api/symptoms_idor_regression_test.go](internal/api/symptoms_idor_regression_test.go) |
| A restore whose `other_symptoms` name collides with another owner's custom symptom resolves to the importing owner's own row, never the other owner's id, and leaves that owner's catalog and logs untouched | `TestImportServiceScopesResolvedSymptomsToImportingOwner` in [internal/services/import_service_test.go](internal/services/import_service_test.go) |
| The pending TOTP enrollment cookie is bound to the account that started the enrollment: an owner id is mandatory at seal time, and on read a payload minted for a different account — or one naming no account at all — is refused and the cookie cleared, so another owner's pending secret can never be enrolled as this account's credential | `TestVerifyTOTP2FAEnrollmentRefusesSetupCookieNotMintedForThisOwner` in [internal/api/handlers_settings_2fa_test.go](internal/api/handlers_settings_2fa_test.go); the seal-time refusal and both zero-id operands of the shared predicate in `TestTOTPSetupCookieRefusesUnattributedOwner` ([internal/api/totp_cookies_test.go](internal/api/totp_cookies_test.go)) |
| Provider-logout state is resolved and consumed only by the **pairing** of session id and owner, on every path — including the bridge redirect, which runs after the session is gone and takes the owner from the sealed bridge cookie rather than resolving the session id alone. An owner id is mandatory at seal time; a payload naming none is invalid on read, refused and retracted, so it degrades to a local `/login` sign-out instead of matching whichever account that session id belongs to. A cookie presenting one owner with another owner's session id reaches no provider redirect and leaves that owner's row unconsumed | `TestOIDCLogoutBridgeRedirectRefusesAnotherOwnersSessionID` and `TestOIDCLogoutBridgeRedirectDegradesALegacyBridgeCookieToLocalSignOut` in [internal/api/auth_oidc_v2_regressions_test.go](internal/api/auth_oidc_v2_regressions_test.go); the seal-time refusal and the read-time retraction in `TestOIDCLogoutBridgeCookieRefusesToMintWithoutAnOwner` and `TestOIDCLogoutBridgeCookieRefusesAnUnattributedPayloadAndRetractsIt` ([internal/api/oidc_logout_cookie_test.go](internal/api/oidc_logout_cookie_test.go)); the repository and service predicates beneath them in `TestOIDCLogoutStateRepositoryScopesReadsAndDeletesToTheOwner` ([internal/db/ttl_repositories_test.go](internal/db/ttl_repositories_test.go)) and `TestOIDCLogoutStateServiceRefusesARecordThatIsNotTheAskingOwners` ([internal/services/oidc_logout_state_owner_scope_test.go](internal/services/oidc_logout_state_owner_scope_test.go)) |
| A one-time reveal surface (recovery code, calendar-feed subscribe URL) only ever reveals to the owner its sealed payload was minted for. An owner id is mandatory at seal time, and a payload naming no owner is **invalid** on read rather than a comparison that is skipped for want of an operand — so a well-sealed but unattributed payload reveals nothing to whichever session presents it, and is cleared instead of left presentable on a retry | `TestRecoveryCodePageRefusesUnattributedRecoveryCookie` in [internal/api/auth_recovery_code_route_regressions_test.go](internal/api/auth_recovery_code_route_regressions_test.go), `TestCalendarFeedRevealRefusesUnattributedCookie` in [internal/api/settings_calendar_feed_regressions_test.go](internal/api/settings_calendar_feed_regressions_test.go); the seal-time refusal and both zero-id operands of the shared predicate in `TestRecoveryCodeCookieRefusesUnattributedOwner` ([internal/api/recovery_code_cookie_encoding_regression_test.go](internal/api/recovery_code_cookie_encoding_regression_test.go)) and `TestCalendarFeedRevealCookieRefusesUnattributedOwner` ([internal/api/calendar_feed_reveal_cookie_test.go](internal/api/calendar_feed_reveal_cookie_test.go)) |

### Data Import (Restore)

| Claim | Enforced by |
| --- | --- |
| The JSON restore (`POST /api/v1/imports/json`) is owner-only and CSRF-protected: a missing token returns 403; a valid token imports | `TestImportJSONRejectsMissingCSRF`, `TestImportJSONSucceedsWithCSRF` in [internal/api/imports_regressions_test.go](internal/api/imports_regressions_test.go) |
| Restore is additive — a day that already exists is never overwritten or deleted | `TestImportServiceSkipsExistingDays` in [internal/services/import_service_test.go](internal/services/import_service_test.go) |
| Imported records are re-validated and owner-scoped; round-tripping an export reproduces the same entries | `TestImportServiceRoundTripPreservesEntries` in [internal/services/import_service_test.go](internal/services/import_service_test.go) |
| A crafted file cannot persist an inconsistent anchor (`cycle_start` on a non-period day) | `TestImportServiceDropsCycleStartOnNonPeriodDay` in [internal/services/import_service_test.go](internal/services/import_service_test.go) |
| Custom-symptom creation on import is bounded (`MaxImportCustomSymptoms`), so a crafted file cannot force unbounded catalog growth / DB churn | `TestImportServiceCapsCustomSymptomCreation` in [internal/services/import_service_test.go](internal/services/import_service_test.go) |
| A compressed upload that only crosses the transport body cap once decompressed is refused at the transport layer with the mapped `413 request_too_large`: the import pipeline is never entered, nothing is persisted, and the rejection is never reported as a malformed file — while a body at the cap still restores normally | `TestImportJSONRejectsGzipBodyExceedingDecompressedCap`, `TestImportJSONAcceptsGzipBodyAtTheDecompressedCap` in [internal/api/imports_regressions_test.go](internal/api/imports_regressions_test.go) |

### Webhook Notifications (outbound egress)

The request-free notify pass POSTs due period/ovulation reminders to an owner-configured URL. The endpoint is fully owner-controlled (self-hosted ntfy/Gotify commonly on the LAN), so the request envelope is hardened rather than the destination blanket-blocked. Owners configure the URL from the settings page (`POST /api/v1/users/current/webhook`), where the URL is treated as a write-only secret.

**Deployment posture.** Private/loopback/link-local egress is allowed by default so self-hosted-on-LAN setups (ntfy/Gotify next to the host) work out of the box; the destination is hardened rather than blanket-blocked. This is safe on a single-owner instance, but on a multi-owner or publicly reachable instance with `REGISTRATION_MODE=open` any account that signs up can point its webhook at an internal address and use the shared notify pass to probe your private network (SSRF). For those deployments set `WEBHOOK_BLOCK_PRIVATE_ADDRESSES=true`, or close sign-ups with `REGISTRATION_MODE=closed`. The server emits a startup warning when `REGISTRATION_MODE=open` and `WEBHOOK_BLOCK_PRIVATE_ADDRESSES=false`, so the trade-off is visible in the logs at boot.

| Claim | Enforced by |
| --- | --- |
| The webhook settings save is OwnerOnly + CSRF-protected and scoped to the session's owner — one owner's save can never write another owner's row | `TestWebhookSaveScopedToOwner` in [internal/api/settings_webhook_regressions_test.go](internal/api/settings_webhook_regressions_test.go), the `POST /api/v1/users/current/webhook` cases in `TestUnsupportedRoleRejectedAcrossEveryAuthedV1Route` ([owner_only_coverage_regression_test.go](internal/api/owner_only_coverage_regression_test.go)) and `TestStateMutatingEndpointsRejectMissingCSRFToken` ([state_mutation_csrf_missing_token_test.go](internal/api/state_mutation_csrf_missing_token_test.go)) |
| The webhook URL is a write-only secret: after it is stored, the settings page HTML never renders the URL, its token, or its path/query — only a configured/not-configured status plus at most the hostname | `TestWebhookSettingsPageNeverRendersStoredURL` in [internal/api/settings_webhook_regressions_test.go](internal/api/settings_webhook_regressions_test.go); `TestBuildWebhookURLDisplay` in [internal/services/webhook_settings_service_test.go](internal/services/webhook_settings_service_test.go) |
| A saved webhook URL is encrypted at rest (ciphertext in `webhook_url`, never plaintext) and round-trips back only through the aad-bound decrypt | `TestWebhookSaveEncryptsURLAtRestAndRoundTrips` in [internal/api/settings_webhook_regressions_test.go](internal/api/settings_webhook_regressions_test.go) |
| A non-`http(s)` webhook URL is rejected at save with `400` and nothing is persisted (scheme guard reused from the service, not re-implemented in transport) | `TestWebhookSaveNonHTTPSchemeRejectedNothingPersisted` in [internal/api/settings_webhook_regressions_test.go](internal/api/settings_webhook_regressions_test.go), `TestValidateWebhookURL` in [internal/services/webhook_settings_service_test.go](internal/services/webhook_settings_service_test.go) |
| A blank URL submission leaves the stored endpoint unchanged (write-only field); a distinct remove affordance clears it and forces delivery off | `TestWebhookSaveBlankURLKeepsStoredEndpoint`, `TestWebhookSaveRemoveClearsStoredEndpoint` in [internal/api/settings_webhook_regressions_test.go](internal/api/settings_webhook_regressions_test.go); `TestSaveWebhookSettingsFromFormBlankURLKeepsStoredEndpoint`, `TestSaveWebhookSettingsFromFormRemoveClearsAndDisables` in [internal/services/webhook_settings_service_test.go](internal/services/webhook_settings_service_test.go) |
| In a multi-owner batch, each owner's reminder is delivered only to that owner's URL — one owner's predicted health data never reaches another owner's URL | `TestNotifyCrossOwnerIsolation`, `TestNotifyCrossOwnerHealthDataStaysScoped` in [internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go) |
| Delivery refuses ALL redirects, so a 3xx cannot steer the request (or its body) to a second unvalidated origin | `TestWebhookDeliveryRefusesRedirect` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go) |
| Only `http`/`https` schemes are delivered; other schemes are rejected at delivery time (defence in depth over save-time validation) | `TestWebhookDeliveryRejectsNonHTTPScheme` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go) |
| With the off-by-default `WEBHOOK_BLOCK_PRIVATE_ADDRESSES` gate on, a hostname that resolves to any private/loopback/link-local/unspecified address — including RFC 6598 CGNAT (`100.64.0.0/10`, which `net.IP.IsPrivate()` omits), RFC 1122 "this network" (`0.0.0.0/8`, which `IsUnspecified()` matches only at `0.0.0.0`), deprecated IPv6 site-local (`fec0::/10`, outside Go's `fc00::/7`) and the RFC 8215 local-use NAT64 block (`64:ff9b:1::/48`, private wholesale), plus every remaining prefix the IANA special-purpose registries record as not globally reachable — RFC 2544 benchmarking (`198.18.0.0/15`), IETF protocol assignments (`192.0.0.0/24`), documentation (`192.0.2.0/24`, `198.51.100.0/24`, `203.0.113.0/24`, `2001:db8::/32`, `3fff::/20`), reserved `240.0.0.0/4` (which contains the limited broadcast address), all multicast, and the IPv6 discard-only, benchmarking, ORCHIDv2, drone-remote-ID and SRv6-SID blocks — while the special-purpose prefixes those registries record as globally reachable (AS112, AMT, the `2001:1::1-3` anycast trio) stay deliverable — with the deliberate exception of the PCP/TURN anycast addresses `192.0.0.9` and `192.0.0.10`, refused with the enclosing `192.0.0.0/24` rather than carved out of it — plus every IPv6 transition form that embeds an IPv4 destination — RFC 6052 NAT64 (`64:ff9b::/96`), 6to4 (`2002::/16`), Teredo (`2001::/32`, both its server address and its bitwise-inverted client address), IPv4-compatible (`::/96`) and SIIT IPv4-translated (`::ffff:0:0:0/96`) — each decoded and classified by the IPv4 it wraps, so a wrapped private v4 is blocked while a wrapped public v4 stays allowed, and terminal classification runs before decoding so `::1` and `::` (both inside `::/96`) are never re-read as public — is refused before the request, and the IP dialed is one of the validated records — no second, independent resolution (DNS-rebinding TOCTOU closed); a mixed public/private answer is rejected and the gate-off default path is unchanged | `TestWebhookDeliveryBlocksHostnameResolvingToPrivate`, `TestWebhookDeliveryBlocksHostnameResolvingToCarrierRanges`, `TestWebhookDeliveryAllowsHostnameResolvingToPublic`, `TestWebhookDeliveryBlocksHostnameResolvingToSpecialPurposeRanges`, `TestWebhookDeliveryBlocksMixedPublicPrivateAnswer`, `TestIsPrivateIP` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go), and the registry walk that decides which ranges those are — `TestIsPrivateIPWalksTheSpecialPurposeRegistries`, `TestGloballyReachableControlsStayDeliverable` in [internal/services/webhook_delivery_special_purpose_test.go](internal/services/webhook_delivery_special_purpose_test.go). The two sub-claims those delivery-level tests cannot observe are pinned separately in the same file: the anti-TOCTOU dial — that the address handed to the dialer is the record just validated — by the `"resolved public host dials the validated IP"` subtest of `TestGuardedDialContext` (whose siblings also pin fail-closed behaviour on a resolver error and on an empty answer), and the untouched default path by `TestWebhookDeliveryGateOffLeavesStockPathForHostname` |
| A hostile endpoint cannot stall the pass (context/dialer timeout) or force an unbounded body read (response capped by `io.LimitReader`) | `TestWebhookDeliveryHonorsContextTimeout`, `TestWebhookDeliveryHandlesOversizedBody` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go) |
| Nor an unbounded response HEADER block, which is read before the body cap can apply and otherwise takes Go's 10 MiB default: `MaxResponseHeaderBytes` is set explicitly, with the TLS handshake and the header wait each separately bounded so one phase cannot spend the whole delivery budget | `TestWebhookDeliveryRefusesOversizedResponseHeaders` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go) — its second half is the positive anchor, an ordinary header set on the same server still succeeding, so the test cannot pass by refusing everything |
| A webhook URL must name a host and, when it carries one, an in-range port (`1..65535`) — checked when the endpoint is saved AND again before delivery, since save-time validation never revisits a stored row. `url.Parse` gives a port-only authority (`http://:8080/`) a non-empty `Host` with an empty `Hostname()`, which Go's dialer reads as the unspecified address, i.e. the local machine | `TestValidateWebhookURL` in [internal/services/webhook_settings_service_test.go](internal/services/webhook_settings_service_test.go) for the save path, `TestWebhookDeliveryRejectsHostlessAuthority` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go) for the delivery boundary — the latter runs with the private-address gate deliberately OFF, since with it on the guarded resolver refuses an empty hostname anyway and would hide whether this check does anything |
| Delivery logs and returns the destination HOST only — never the full URL, path, query, or userinfo (which may carry an ntfy token) | `TestWebhookDeliveryLogsHostOnly` in [internal/services/webhook_delivery_test.go](internal/services/webhook_delivery_test.go) |
| Every delivered payload carries the mandatory medical-safety disclaimer | `TestNotifyDisclaimerPresentInEveryPayload` in [internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go) |
| The per-kind watermark is claimed by a compare-and-set BEFORE the delivery it covers, so a reminder ships once per cycle even when two passes overlap — the scheduler goroutine and an operator `ovumcy notify` process both compute their due-set from a record snapshot, and only one of them can win the claim; the loser skips without delivering. A failed delivery releases the claim, so the watermark is left unchanged and the next pass retries, and a later pass over a standing watermark sends nothing (idempotency in all three directions) | `TestNotifyWatermarkAdvancesOnlyOnSuccess`, `TestNotifyIdempotentSecondPassSkips`, `TestNotifyRetriesAfterFailure`, `TestNotifyLostClaimSkipsWithoutDelivering`, `TestNotifyFailedDeliveryReleasesTheClaim` in [internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go); `TestClaimWebhookWatermarkCanonicalizesToUTCMidnight`, `TestClaimWebhookWatermarkScopedToUser`, `TestClaimWebhookWatermarkIsExclusivePerAnchor`, `TestReleaseWebhookWatermarkRestoresOnlyItsOwnClaim` in [internal/db/user_repository_webhook_test.go](internal/db/user_repository_webhook_test.go); the overlap itself in `TestConcurrentNotifyPassesDeliverOnce` in [internal/reminders/idempotency_layering_test.go](internal/reminders/idempotency_layering_test.go) |
| A revocation the owner completed — disabling delivery, replacing or removing the endpoint, changing the shared reminder lead window, or clearing all data — holds against a notify pass that is already running. The pass decides from one snapshot read minutes before the request leaves, and none of those writes moves a watermark (a settings save deliberately leaves them alone, and a clear-data wipe NULLs them, which re-opens the first-ever-send branch), so each of them advances the owner's monotonic `webhook_config_version` in the same statement that performs it, and the pre-delivery claim pins the epoch its own snapshot carried: a pass holding a revoked configuration loses the claim and POSTs nothing. Clear-data advances that counter rather than resetting it, since a reset would hand the stale snapshot its own value back. `webhook_enabled` and the kind's own opt-in are pinned alongside it as the persistence layer's own floor: no reachable path needs them today, since the pass returns before the claim for an owner whose snapshot says delivery is off, and they are there against a future caller that does not, or a write of those flags that skips the epoch. What is NOT promised is the recall of a request already on the wire: the line held is that no new request begins under a revoked configuration | `TestClaimWebhookWatermarkIsLostAfterTheOwnerDisabledDelivery`, `TestClaimWebhookWatermarkIsLostAfterTheOwnerChangedTheEndpoint`, `TestClaimWebhookWatermarkIsLostAfterClearData`, `TestClaimWebhookWatermarkIsRefusedWhileDeliveryIsDisabled`, `TestClaimWebhookWatermarkIsRefusedForAKindTheOwnerOptedOutOf`, `TestClearAllDataAdvancesTheWebhookRevocationEpoch`, `TestSaveWebhookSettingsAdvancesTheRevocationEpoch`, `TestUpdateReminderLeadDaysAdvancesTheRevocationEpoch`, and the release side the mechanism rests on in `TestReleaseWebhookWatermarkStillRestoresAfterARevocation` and `TestReleaseWebhookWatermarkLeavesAClearDataWipeAlone` in [internal/db/user_repository_webhook_revocation_test.go](internal/db/user_repository_webhook_revocation_test.go); the pass pinning its OWN snapshot's epoch in `TestNotifyClaimsAgainstTheRevocationEpochItsSnapshotCarried` ([internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go)); and the interleaving end to end — real repository, real pass, revocation landing between the snapshot and the claim — in `TestNotifyPassCannotDeliverAfterTheOwnerRevoked`, with `TestNotifyPassDeliversWhenTheConfigurationStands` as its positive control, in [internal/services/webhook_notify_revocation_integration_test.go](internal/services/webhook_notify_revocation_integration_test.go) |
| A `webhook_url` that fails to decrypt (e.g. after `SECRET_KEY` rotation) fails safe: that owner is skipped, never delivered to a garbage target, and the pass continues | `TestNotifyDecryptFailureSkipsOwner` in [internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go) |
| `--dry-run` computes what would be sent but makes no outbound request and writes no watermark | `TestNotifyDryRunMakesNoRequestOrWatermark` in [internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go), `TestRunNotifyCommandDryRunPropagates` in [internal/cli/notify_test.go](internal/cli/notify_test.go) |
| `ovumcy notify` prints no health specific by default: the `--dry-run` preview names the owner, the number of pending reminders, and the destination host, but never a reminder type or an estimated date — so a scheduled pass redirected into an operator log records no prediction about an identified owner. The explicit `--show-health-details` opt-in is the only way to print them | `TestRunNotifyCommandDryRunOmitsHealthSpecificsByDefault`, `TestRunNotifyCommandDryRunShowsHealthSpecificsOnRequest`, `TestRunNotifyCommandDeliveryPassIgnoresHealthDetailsFlag` in [internal/cli/notify_test.go](internal/cli/notify_test.go) |
| The endpoint URL reaches `ovumcy webhook set` from exactly one out-of-band source: supplying both `OVUMCY_WEBHOOK_URL` and `--url-stdin` is refused before the database is opened, so a stale ambient export can never outrank a piped URL and arm an owner's reminders at an endpoint the operator never typed | `TestRunWebhookCommandEnvAndStdinRejected`, `TestRunWebhookCommandStdinAloneStillReadsStdin`, `TestRunWebhookCommandURLFromEnv` in [internal/cli/webhook_test.go](internal/cli/webhook_test.go) |

### Calendar Feed Subscription (.ics)

The read-only `.ics` feed lets a calendar client subscribe to an owner's upcoming predicted period/ovulation reminders. A calendar client sends no cookie, so the subscribe URL carries a bearer capability token in its path — the single sanctioned exception to *no usable secret in transport* for a value usable **on its own**. (The TOTP enrollment seed, shown once on the enrollment page, is the only other declared exception; it is inert until the account confirms its first code — see *Password & Auth Policy* below.) It is a capability token, not an auth credential (read-only, owner-scoped, revocable, does not bump `auth_session_version`), compensated by keyed hashing-at-rest, 404-no-oracle, log-redaction, per-IP rate-limiting, and a write-only shown-once subscribe URL. Owners manage it from the settings page (`POST /api/v1/users/current/calendar-feed`, `POST …/calendar-feed/rotate`, `DELETE …/calendar-feed`).

| Claim | Enforced by |
| --- | --- |
| The verifier half of the token is never stored in plaintext: the DB row holds a keyed MAC of it (`HMAC-SHA256` under a `SECRET_KEY`-derived key, what verification compares) plus a bcrypt hash (what a rollback to a pre-migration-032 binary reads). Neither is usable without the other half of the pair it authenticates, and the MAC is worthless without `SECRET_KEY` | `TestGenerateCalendarFeedTokenHashAtRest`, `TestGenerateCalendarFeedTokenMintsBothVerifierColumns` in [internal/services/calendar_feed_token_test.go](internal/services/calendar_feed_token_test.go), `TestCalendarFeedTokenHashAtRestInDBRow` in [internal/db/user_repository_calendar_feed_test.go](internal/db/user_repository_calendar_feed_test.go), `TestGenerateCalendarFeedTokenVerifierIsRealBcrypt` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go) |
| Verification compares the keyed MAC, not bcrypt: the verifier carries 160 bits from `crypto/rand`, so a work factor buys no guessing resistance while costing ~265 ms of CPU per request on an unauthenticated endpoint. A stored MAC that does not match is a hard refusal, never re-checked against bcrypt — so rotating `SECRET_KEY` disarms armed feeds (the owner re-generates the subscribe URL) instead of keeping a stale authenticator alive | `TestVerifyCalendarFeedTokenPrefersMACOverBcrypt` in [internal/services/calendar_feed_token_test.go](internal/services/calendar_feed_token_test.go), `TestResolveFeedVerifiesThroughMACNotBcrypt`, `TestResolveFeedRefusesStaleMACWithoutBcryptFallback` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go) |
| A row minted before migration 032 has no MAC (it cannot be derived from the hash) and still verifies through bcrypt; the MAC is written in during the first request that presents the correct token, scoped by a compare-and-set to that owner and that selector so a concurrent rotation can never pair an old MAC with a new hash, and a failed write never withholds a feed whose token verified | `TestVerifyCalendarFeedTokenFallsBackToBcryptOnlyForPre032Rows` in [internal/services/calendar_feed_token_test.go](internal/services/calendar_feed_token_test.go), `TestResolveFeedBackfillsMACForPre032RowAndLeavesBcryptBehind`, `TestResolveFeedRefusesAWrongVerifierOnAPre032RowAndWritesNoMAC` (the wrong-verifier leg), `TestResolveFeedServesFeedWhenMACBackfillFails` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go), `TestBackfillCalendarFeedVerifierMACWritesOnlyIntoAnEmptyColumn`, `TestBackfillCalendarFeedVerifierMACRefusesAfterRotation`, `TestBackfillCalendarFeedVerifierMACScopedToUser` in [internal/db/user_repository_calendar_feed_test.go](internal/db/user_repository_calendar_feed_test.go), `TestCalendarFeedMigratesPre032RowOnFirstSuccessfulPoll` in [internal/api/calendar_feed_regressions_test.go](internal/api/calendar_feed_regressions_test.go) |
| A feed armed before migration 032 does not survive a `SECRET_KEY` rotation either: its bcrypt hash never depended on the key, so a boot-time sentinel compares the stored calendar-feed key epoch (`app_state.calendar_feed_key_epoch`) with the running key's and disarms every armed no-MAC row — before the listener starts, so no poll can re-arm a leaked URL through the MAC backfill. A **missing** stored epoch is not a quiet third case: an upgrade from a release that predates the sentinel reaches it holding exactly those rows with no evidence of which key minted them, so they are disarmed there too rather than adopted as a baseline — a new installation holds none and nothing is disarmed | `TestCalendarFeedRotationSentinelRotationDisarmsThenRecordsNewEpoch`, `TestCalendarFeedRotationSentinelFirstBootOnANewInstallationDisarmsNothing`, `TestCalendarFeedRotationSentinelFirstBootAfterAnUpgradeDisarmsTheMACLessFeeds`, `TestCalendarFeedRotationSentinelFirstBootKeepsTheEpochUnwrittenWhenTheDisarmFails`, `TestCalendarFeedRotationSentinelFailedDisarmKeepsOldEpoch` in [internal/services/calendar_feed_rotation_sentinel_test.go](internal/services/calendar_feed_rotation_sentinel_test.go), `TestDisarmCalendarFeedTokensWithoutMACOnlyTouchesLegacyArmedRows` in [internal/db/user_repository_calendar_feed_disarm_test.go](internal/db/user_repository_calendar_feed_disarm_test.go), `TestMustEnforceCalendarFeedKeyRotationAcrossBoots`, `TestFirstBootAfterAnUpgradeDisarmsTheLegacyFeedItInherits` in [cmd/ovumcy/calendar_feed_rotation_boot_test.go](cmd/ovumcy/calendar_feed_rotation_boot_test.go) |
| A restore of a backup taken before a revocation does not bring the feed back either. The key epoch above cannot see that case — it lives in `app_state`, so a restore returns it together with the feed columns and the two always agree — so a second marker, the **restore fence**, keeps its other half OUTSIDE the database (`CALENDAR_FEED_FENCE_PATH`, a mount no database backup captures) and is advanced by every write that arms, rotates or removes a feed. A restored database therefore carries a marker older than the file's, and the boot pass disarms every armed row, MAC or not, before the listener starts. With no fence available the pass fails closed: every armed feed is disarmed on each start | `TestCalendarFeedRestoreFenceDisarmsWhenTheHalvesDisagree`, `TestCalendarFeedRestoreFenceAdvanceMovesBothHalvesTogether`, `TestCalendarFeedRestoreFenceWithoutAnAnchorDisarmsAndRecordsNoToken` in [internal/services/calendar_feed_restore_fence_test.go](internal/services/calendar_feed_restore_fence_test.go), `TestEveryCalendarFeedWriterAdvancesTheRestoreFence`, `TestDisarmAllCalendarFeedTokensClearsEveryArmedRowIncludingMACCarriers` in [internal/db/calendar_feed_fence_writers_guard_test.go](internal/db/calendar_feed_fence_writers_guard_test.go), `TestDocumentedVolumeRestoreLeavesARevokedCalendarFeedRevoked`, `TestDocumentedPostgresRestoreLeavesARevokedCalendarFeedRevoked` in [scripts/backuprestoredoc/calendar_feed_restore_fence_test.go](scripts/backuprestoredoc/calendar_feed_restore_fence_test.go) |
| A server that started **without** a usable fence stamps its database as having run unanchored, and that stamp — not the two halves, which are both empty in this case — is what makes the first start WITH a fence disarm rather than adopt the rows it finds. Without it, an instance that served unfenced, was backed up, had a feed revoked by its owner and was then restored under a freshly mounted fence would serve the revoked `.ics` URL again. The stamp is read ONLY where both halves are empty, so a stale one can never turn an ordinary restart into a disarm-all, and the boot that answers for it erases it | `TestCalendarFeedRestoreFenceUnanchoredBootStampsTheDatabase`, `TestCalendarFeedRestoreFenceEmptyHalvesOverAStampedDatabaseDisarm`, `TestCalendarFeedRestoreFenceIgnoresAStaleStampWhenTheHalvesAgree`, `TestCalendarFeedRestoreFenceStampErasureFailureFailsTheBoot`, `TestCalendarFeedRestoreFenceUnanchoredStampFailureFailsTheBoot` in [internal/services/calendar_feed_restore_fence_test.go](internal/services/calendar_feed_restore_fence_test.go), `TestAppStateRepositoryDeleteRemovesTheKeyAndToleratesAMissingOne` in [internal/db/app_state_repository_test.go](internal/db/app_state_repository_test.go), and end to end over the documented backup and restore, polling the old subscribe URL over HTTP, `TestVolumeRestoreOfAnUnanchoredBackupDisarmsOnTheFirstFencedBoot`, `TestPostgresRestoreOfAnUnanchoredBackupDisarmsOnTheFirstFencedBoot` in [scripts/backuprestoredoc/calendar_feed_restore_fence_test.go](scripts/backuprestoredoc/calendar_feed_restore_fence_test.go) |
| Both halves of that fence move under one lock, and the two writes that ARE a revocation (mint/rotate, revoke) advance it BEFORE their row write. Interleaved advances would leave the halves holding different tokens — a restore nobody performed, answered by disarming every armed feed — and a fence left behind its row is the window a restore crosses | `TestCalendarFeedRestoreFenceAdvanceIsMutuallyExclusive`, `TestCalendarFeedRestoreFenceConcurrentAdvancesLeaveTheHalvesAgreeing` in [internal/services/calendar_feed_restore_fence_test.go](internal/services/calendar_feed_restore_fence_test.go), `TestEveryCalendarFeedWriterAdvancesTheRestoreFence` (ordering both ways) in [internal/db/calendar_feed_fence_writers_guard_test.go](internal/db/calendar_feed_fence_writers_guard_test.go), `TestEveryBundledStackMountsTheCalendarFeedFence` in [scripts/examplecompose/calendar_feed_fence_mount_test.go](scripts/examplecompose/calendar_feed_fence_mount_test.go) |
| The operator CLI is a third writer of that same fence, and unlike the server's own boot-time pass it cannot fall back on disarming on the next start: `ovumcy reset-password` (a forced reset) and `ovumcy users delete` each remove a calendar feed outside any request the server itself sees, so each confirms the two halves already agree and advances them — through `CalendarFeedRestoreFence.AdvanceConfirmed`, never a bare warning — immediately before its own destructive write, and refuses outright, changing neither half, whenever it cannot: the path unset, relative, or unreachable from this process; the database marker unreadable; the two halves disagreeing; the database holding a marker no fence file is visible for; or neither half ever having recorded one. The one shape that already left something changed — the file half moved and the database write then failed — is named apart from every other refusal's "Nothing was changed" and points at a server restart rather than a bare rerun, which would only read the same stale database marker again | `TestConfirmOperatorFeedRevocationSucceedsAndAdvancesWhenTheHalvesAgree`, `TestConfirmOperatorFeedRevocationRefusesAndWritesNothing`, `TestConfirmOperatorFeedRevocationNamesTheHalfAdvancedFenceInsteadOfClaimingNothingChanged`, `TestConfirmOperatorFeedRevocationHalfAdvancedRemedyIsAServerBootNotABareRerun`, `TestConfirmOperatorFeedRevocationNamesTheAnchorMissingWhenTheDatabaseIsArmedButNoFileIsVisible`, `TestConfirmOperatorFeedRevocationRejectsANilFenceInsteadOfPanicking`, `TestCalendarFeedFenceConfirmRefusalHasACaseForEveryDefinedState`, `TestCalendarFeedFenceConfirmRefusalPanicsOnAnUnhandledState` in [internal/cli/repositories_test.go](internal/cli/repositories_test.go), `TestUsersDeleteRefusesAndDeletesNothingWithoutAConfirmedFence`, `TestUsersDeleteAdvancesTheFenceBeforeTheRow`, `TestResetPasswordRefusesWithoutAConfirmedFenceAndLeavesTheHashAlone` in [internal/cli/calendar_feed_revocation_gate_test.go](internal/cli/calendar_feed_revocation_gate_test.go); `TestCalendarFeedRestoreFenceAdvanceConfirmedAdvancesBothHalvesWhenTheyAgree`, `TestCalendarFeedRestoreFenceAdvanceConfirmedRefusesWithoutAnAnchorAndWritesNothing`, `TestCalendarFeedRestoreFenceAdvanceConfirmedRefusesWhenTheAnchorWriteFailsAndLeavesTheDatabaseHalfAlone`, `TestCalendarFeedRestoreFenceAdvanceConfirmedRefusesWhenTheHalvesAreNotAKnownAgreeingPairAndWritesNothing`, `TestCalendarFeedRestoreFenceAdvanceConfirmedPropagatesADatabaseFailureAfterTheFileHasAlreadyMoved`, `TestCalendarFeedRestoreFenceAdvanceConfirmedRefusesWhenTheMarkerCannotBeReadAndWritesNothing`, `TestCalendarFeedRestoreFenceAdvanceConfirmedSharesTheMutexWithAdvance` in [internal/services/calendar_feed_restore_fence_test.go](internal/services/calendar_feed_restore_fence_test.go); and end to end over the documented backup and restore, running the real binary as its own process and polling the old subscribe URL over HTTP, `TestOperatorRevocationSurvivesTheDocumentedVolumeRestore`, `TestOperatorRevocationSurvivesTheDocumentedPostgresRestore` in [scripts/backuprestoredoc/operator_calendar_feed_revocation_test.go](scripts/backuprestoredoc/operator_calendar_feed_revocation_test.go) |
| A malformed token, unknown selector, wrong verifier, and disabled feed all return an identical 404 with the fixed status-text body `Not Found` (nothing cause-specific, no oracle). The selector-miss path re-executes the real verification against a placeholder MAC so it cannot be timed apart from a real verifier check — equalization tracks whatever primitive verification uses instead of hardcoding its own; a malformed token is rejected before the lookup and deliberately spends none — its shape is already known to whoever sent it, so it reveals nothing about selector existence. **Transitional exception:** a not-yet-migrated pre-032 row still costs one bcrypt, so its wrong-verifier latency is distinguishable from a selector miss until its first successful poll. Reaching that signal requires already holding a valid 80-bit selector (a leaked subscribe URL carries both halves, so there is no realistic path to the selector alone), and the feed's own per-IP budget caps the attempt rate | `TestCalendarFeedReturnsBare404WithoutOracleForBadTokens` in [internal/api/calendar_feed_regressions_test.go](internal/api/calendar_feed_regressions_test.go), `TestResolveFeedIdenticalNotFoundForEveryBadToken`, `TestResolveFeedEqualizesTimingOnSelectorMiss`, `TestCalendarFeedTimingEqualizationPlaceholderReachesTheVerifierCompare` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go) |
| The feed token value never reaches a request log — the matched-route template masks it, and the raw-path fallback masks an `<opaque-token>.ics` segment | `TestCalendarFeedTokenIsRedactedFromRequestLog`, `TestSanitizeRequestLogPathMasksRawTokenDotICSFallback` in [internal/api/calendar_feed_regressions_test.go](internal/api/calendar_feed_regressions_test.go) |
| The route carries no `Set-Cookie` on any outcome — 200, 404, or the limiter's own 429 — on GET or HEAD, on any spelling of the URL the router treats as the same route (case-folded, trailing slash alike). Both app-wide middlewares that would otherwise mint a cookie on any safe method lacking a matching one are excluded for this route (the shared predicate is `api.IsCalendarFeedRequest`): the CSRF middleware's own token-issuance side effect (`csrfMiddlewareConfig.Next` in `cmd/ovumcy/server.go`) and `LanguageMiddleware`'s timezone-cookie persistence. The feed's own rate limiter is scoped to the same route, so a bare prefix, a nested path segment, or a non-GET/HEAD verb under it spends no part of this budget either. Neither exclusion touches CSRF validation, which a safe method was never subject to, so the mutating exemption count in the *Cookies* rows below stays exactly one | `TestCalendarFeedRouteSetsNoCookieOnTheProductionStack`, `TestCalendarFeedRouteSetsNoCookieForAnArmedFeed`, `TestCalendarFeedRouteSetsNoCookieOn429`, `TestCalendarFeedOverMatchPathsKeepLanguageCatalogueAndCSRFSupport`, `TestCalendarFeedBodyIgnoresRequestTimezoneSignals` in [cmd/ovumcy/calendar_feed_no_cookie_test.go](cmd/ovumcy/calendar_feed_no_cookie_test.go), `TestCalendarFeedLimiterCountsEveryRoutableSpellingAndVerbOfTheFeedURL`, `TestCalendarFeedLimiterSpendsNoBudgetOnPathsThatReachNoFeed`, `TestEveryLimiterScopePredicateIsOneTheSweepsCover` in [cmd/ovumcy/rate_limit_scope_guard_test.go](cmd/ovumcy/rate_limit_scope_guard_test.go), `TestCSRFPredicateSkipsTheCookielessFeedGETAndHEADWithoutWideningTheMutatingExemptions` in [cmd/ovumcy/csrf_exemption_guard_test.go](cmd/ovumcy/csrf_exemption_guard_test.go), `TestLanguageMiddlewareSkipsTheCookielessCalendarFeedEntirely` in [internal/api/middleware_language_helpers_mutkill_test.go](internal/api/middleware_language_helpers_mutkill_test.go), `TestIsCalendarFeedRequestMatchesTheRouteFormAndMethod` in [internal/api/handlers_calendar_feed_mutkill_test.go](internal/api/handlers_calendar_feed_mutkill_test.go), `TestIsCalendarFeedRequestMatchesWhatFiberActuallyDispatches` in [cmd/ovumcy/calendar_feed_route_shape_test.go](cmd/ovumcy/calendar_feed_route_shape_test.go) — see `api.IsCalendarFeedRequest`'s own godoc for the exact route-shape rule |
| The feed's not-found path answers outside the app-wide error envelope on purpose — the fixed, cause-free status-text `404` — and it is one of exactly three routes allowed to (`/healthz` and `/readyz` are the others); its per-IP `429` instead answers through the shared limiter path like every other route (the *Rate Limits* rows below own that shape). A route that starts answering a rejection outside the envelope without being declared, or a declared route that stops needing to, fails the sweep | `TestEveryRejectionOutsideTheDeclaredExemptionsCarriesTheEnvelope` in [cmd/ovumcy/error_envelope_regression_test.go](cmd/ovumcy/error_envelope_regression_test.go); the feed's `429` arm is the `calendar feed` case of `TestEveryRateLimiterAnswersThroughTheSharedEnvelope` in [cmd/ovumcy/rate_limit_envelope_regression_test.go](cmd/ovumcy/rate_limit_envelope_regression_test.go) |
| The cookieless feed endpoint is per-IP rate-limited (429 once the budget is spent) | `TestCalendarFeedRouteSetsNoCookieOn429` in [cmd/ovumcy/calendar_feed_no_cookie_test.go](cmd/ovumcy/calendar_feed_no_cookie_test.go), `TestCalendarFeedLimiterCountsEveryRoutableSpellingAndVerbOfTheFeedURL`, `TestCalendarFeedLimiterSpendsNoBudgetOnPathsThatReachNoFeed`, `TestEveryLimiterScopePredicateIsOneTheSweepsCover` in [cmd/ovumcy/rate_limit_scope_guard_test.go](cmd/ovumcy/rate_limit_scope_guard_test.go), `TestRateLimitKeyGeneratorBucketing` in [cmd/ovumcy/rate_limit_keygen_test.go](cmd/ovumcy/rate_limit_keygen_test.go). The first three drive the production middleware chain (`newFiberApp`), where the limiter is the one this budget actually ships behind; the last two carry the **per-IP** half — one pins that every limiter in the package keys on `rateLimitKeyGenerator`, the other that this generator hands two client addresses two buckets, so the cap is per caller rather than one bucket shared by everyone. `TestCalendarFeedIsRateLimitedPerIP` in [internal/api/calendar_feed_regressions_test.go](internal/api/calendar_feed_regressions_test.go) pins the refusal's SHAPE — 429 carrying a `Retry-After` — on a limiter it mounts itself, so it says nothing about the shipped wiring; that is the production-stack tests' half |
| The subscribe URL is a write-only secret: generate/rotate reveal it exactly once and it is never rendered into a later page; a JSON client receives only the reveal `next_path`, never the URL. The sealed one-time cookie decides *which* URL may be shown; what makes the reveal single use is the owner's server-side consumption mark `users.calendar_feed_revealed_at` (migration 036), claimed with a compare-and-set — so replaying the original sealed value on the same session reveals nothing and logs no second `settings.calendar_feed_reveal` egress event | `TestCalendarFeedGenerateRevealsURLOnceAndNeverAgain` (whose replay leg presents the original sealed value), `TestCalendarFeedRevealRefusesAReplayedCookieAndRearmsOnRotate`, `TestCalendarFeedGenerateJSONReturnsRevealPathNotURL` in [internal/api/settings_calendar_feed_regressions_test.go](internal/api/settings_calendar_feed_regressions_test.go), the audit half in `TestCalendarFeedRevealIsAuditedAsHealthEgress` ([internal/api/security_event_logging_egress_regressions_test.go](internal/api/security_event_logging_egress_regressions_test.go)); the sealed reveal cookie is tamper/round-trip pinned in [internal/api/calendar_feed_reveal_cookie_test.go](internal/api/calendar_feed_reveal_cookie_test.go) |
| Rotate mints a new token so the previous URL dies immediately; revoke clears every feed column so the feed 404s | `TestCalendarFeedRotateInvalidatesOldToken`, `TestCalendarFeedRevokeClearsColumns`, `TestCalendarFeedRevokeScopedToOwner` in [internal/api/settings_calendar_feed_regressions_test.go](internal/api/settings_calendar_feed_regressions_test.go), `TestSaveCalendarFeedTokenRotationReplacesRow`, `TestClearCalendarFeedTokenRevokes` in [internal/db/user_repository_calendar_feed_test.go](internal/db/user_repository_calendar_feed_test.go), `TestRevokeFeedTokenClearsColumnsForTheRequestedOwner` in [internal/services/calendar_feed_settings_service_test.go](internal/services/calendar_feed_settings_service_test.go) |
| The feed token is force-cleared in the SAME atomic update that bumps `auth_session_version` on recovery-code regeneration, password reset, forced operator reset, and clear-data; a routine password change keeps it — and a CAS rollback never leaves the feed armed with a stale version | `TestRecoveryCodeRegenForceClearsFeedAtomically`, `TestPasswordResetCASForceClearsFeedAtomically`, `TestForceOperatorResetForceClearsFeedAtomically`, `TestRoutinePasswordChangeDoesNotClearFeed`, `TestPasswordResetCASRollbackLeavesFeedArmedAndVersionUnchanged` in [internal/db/user_repository_calendar_feed_force_clear_test.go](internal/db/user_repository_calendar_feed_force_clear_test.go); `TestClearAllDataResetsCalendarFeedColumns` in [internal/db/user_repository_calendar_feed_test.go](internal/db/user_repository_calendar_feed_test.go) |
| The reveal page itself is a `GET`, which the CSRF middleware never validates, so it is additionally refused for an off-origin initiator, for a destination no client of it has, and for a speculative load (Fetch Metadata) — the published `next_path` fetch stays admitted. Each check reads its own header and fires only on a value the browser stated, so a client sending none of the `Sec-Fetch-*` family is admitted, as it was before the guard. A refused request neither claims the mark nor retracts the sealed cookie — the owner's one reveal survives someone else's link — and is audited as a denied `settings.calendar_feed_reveal` egress only when a reveal cookie was actually presented | `TestCalendarFeedRevealRefusesAForeignRequestWithoutSpendingTheMark`, `TestRefusedCalendarFeedRequestIsAuditedOnlyWhenSomethingCouldBeSpent`, `TestFirstPartyGuardAdmitsAPartiallyStrippedFetchMetadataFamily`, `TestFirstPartyGuardedRoutesAreExactlyTheDeclaredSet` in [internal/api/first_party_guard_regressions_test.go](internal/api/first_party_guard_regressions_test.go) |
| The three settings endpoints are OwnerOnly + CSRF-protected and scoped to the session owner — one owner can never mint, rotate, revoke, or reveal another owner's feed | `TestCalendarFeedGenerateScopedToOwner`, `TestCalendarFeedRevokeScopedToOwner`, `TestCalendarFeedRevealCrossOwnerCookieIgnored` in [internal/api/settings_calendar_feed_regressions_test.go](internal/api/settings_calendar_feed_regressions_test.go), `TestSaveCalendarFeedTokenScopedToUser` in [internal/db/user_repository_calendar_feed_test.go](internal/db/user_repository_calendar_feed_test.go) — whose second leg clears the owner's row and re-reads the other owner's, so it covers revoke as well as the save its name announces; the `calendar-feed` routes are also walked by `TestUnsupportedRoleRejectedAcrossEveryAuthedV1Route` and `TestStateMutatingEndpointsRejectMissingCSRFToken` |
| One owner's token never resolves another owner's data; the feed is owner-scoped end to end | `TestResolveFeedCrossUserIsolation` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go) |
| Every `.ics` event carries the medical-safety disclaimer and a neutral, contentless title (no cycle phase), and predictions are suppressed for pregnancy-pause / unpredictable cycles | `TestBuildCalendarFeedICSEmitsNeutralEventsWithDisclaimer`, `TestBuildCalendarFeedICSSuppressesForPregnancyPause`, `TestBuildCalendarFeedICSSuppressesForUnpredictableCycle` in [internal/services/calendar_feed_ics_test.go](internal/services/calendar_feed_ics_test.go); the resolve path passing the disclaimer into the feed body is pinned by `TestResolveFeedReturnsOwnersFeedForValidToken` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go) |

### Session Invalidation on Credential Rotation

| Claim | Enforced by |
| --- | --- |
| Password change bumps `auth_session_version`; other devices sign out | `auth_password_change_session_regression_test.go` in `internal/api/` |
| Password reset via recovery code bumps `auth_session_version` | `TestResetPasswordAndRotateRecoveryCodeCASRejectsReplay` in [internal/services/password_reset_cas_regression_test.go](internal/services/password_reset_cas_regression_test.go) |
| Recovery-code regeneration bumps `auth_session_version`; originating session refreshed inline, and the reissued cookie authenticates the next request while a cookie issued before the bump does not | `TestAuthServiceRegenerateRecoveryCode` in [internal/services/auth_service_recovery_test.go](internal/services/auth_service_recovery_test.go), `TestRegenerateRecoveryCodeBumpsSessionVersionAndReissuesCookie` in [internal/api/auth_recovery_code_password_required_regressions_test.go](internal/api/auth_recovery_code_password_required_regressions_test.go) |
| Forced `ovumcy reset-password` CLI bumps `auth_session_version` | `TestAuthServiceForceResetPasswordByID` in [internal/services/auth_service_recovery_test.go](internal/services/auth_service_recovery_test.go), `TestRunResetPasswordCommandUpdatesPasswordFromSecurePromptWithoutLeakingPlaintext` in [internal/cli/reset_test.go](internal/cli/reset_test.go) |
| Operator `ovumcy users set-email` re-homes an account's login identity and bumps `auth_session_version` in the same update, under a compare-and-set on the address it was shown; the calendar feed stays armed (a repair is not a compromise event) | `TestSetUserEmailByIDAndRevokeSessionsRepairsALeftoverRow` in [internal/db/user_repository_email_renormalize_test.go](internal/db/user_repository_email_renormalize_test.go), `TestRunUsersCommandSetEmailRestoresTheAccountsTheBootRepairLeavesLockedOut` in [internal/cli/users_test.go](internal/cli/users_test.go) |
| TOTP enable bumps `auth_session_version` and refreshes originating session | `handlers_settings_2fa_session_revocation_test.go` in `internal/api/` |
| TOTP disable bumps `auth_session_version` and refreshes originating session | `handlers_settings_2fa_session_revocation_test.go` in `internal/api/` |
| `clear-data` bumps `auth_session_version` atomically with the wipe — a failure part-way rolls back both, so the account is never left with its health data gone and other devices still signed in | `settings_clear_data_session_revocation_test.go` in `internal/api/`, `TestClearAllDataRollsBackWipeAndVersionBumpTogether` in [internal/db/user_repository_clear_data_prefs_test.go](internal/db/user_repository_clear_data_prefs_test.go) |
| Opportunistic bcrypt-cost rehash on login rewrites `password_hash` without bumping `auth_session_version` (the authenticating session survives) | `TestUpdatePasswordHashOnlyPreservesSessionVersion` in [internal/db/user_repository_cas_test.go](internal/db/user_repository_cas_test.go), `TestAuthenticateCredentialsRehashesStaleCost` in [internal/services/auth_service_hash_cost_test.go](internal/services/auth_service_hash_cost_test.go) |
| `ovumcy_auth` cookie verifies against current `auth_session_version`; revoked sessions are rejected | `TestAuthMiddlewareRejectsRevokedAuthSessionCookieForAPI`, `TestAuthMiddlewareRejectsRevokedAuthSessionCookieAfterForcedResetForHTML` in [internal/api/auth_cookie_compat_regression_test.go](internal/api/auth_cookie_compat_regression_test.go) |

### Cookies

| Claim | Enforced by |
| --- | --- |
| Sealed cookies use the AES-GCM `v2.<...>` envelope | `TestSecureCookieCodecSealsWithVersion2Prefix` in [internal/api/secure_cookie_codec_rotation_test.go](internal/api/secure_cookie_codec_rotation_test.go) |
| Legacy `v1` cookie payloads are rejected | `TestSecureCookieCodecRejectsLegacyV1Payload` in [internal/api/secure_cookie_codec_rotation_test.go](internal/api/secure_cookie_codec_rotation_test.go) |
| Sealed cookies are AAD-bound to the cookie name; cross-cookie substitution fails | `secure_cookie_codec_security_test.go` in `internal/api/` |
| Golden sealed values from the pre-consolidation codec still open for all 11 purposes | `TestSecureCookieCodecOpensPreConsolidationGoldenValues` in [internal/api/secure_cookie_codec_golden_test.go](internal/api/secure_cookie_codec_golden_test.go) |
| `ovumcy_auth`, `ovumcy_register_pickup`, `ovumcy_recovery_code` all set `SameSite=Lax` | `cookie_security_enabled_test.go`, `cookie_security_default_test.go` in `internal/api/` |
| Both one-time reveal cookies (`ovumcy_recovery_code`, `ovumcy_calendar_feed`) carry their 20-minute lifetime **inside** the sealed payload, and each reader verifies that against the clock: the `Expires` attribute is a browser hint, so a client that retained the sealed value is refused on read once it is past the payload's `expires_at`, and a payload carrying **no** expiry is refused the same way rather than displayed. Every such refusal retracts the cookie in that same response and leaves the session alone — the account lands where an absent cookie lands, and re-issues the secret from Settings. The bound is class-wide, not a property of these two: **every** sealed cookie is bounded on the server — by a live expiry in its own payload that its reader refuses once past, by the opaque signed token it wraps and whose `exp` the verifier enforces (`ovumcy_auth`, `ovumcy_reset_password`), or, only where the payload names no account and carries nothing the server generated (`ovumcy_flash`), by nothing at all. A sealed cookie fitting none of the three, or one a sweep cannot classify, is a defect | `TestRecoveryCodePageRefusesAnExpiredRecoveryCookie`, `TestRecoveryCodePageRefusesARecoveryCookieCarryingNoExpiry` in [internal/api/auth_recovery_code_route_regressions_test.go](internal/api/auth_recovery_code_route_regressions_test.go), `TestCalendarFeedRevealRefusesAnExpiredRevealCookie`, `TestCalendarFeedRevealRefusesARevealCookieCarryingNoExpiry` in [internal/api/calendar_feed_reveal_expiry_regression_test.go](internal/api/calendar_feed_reveal_expiry_regression_test.go) — each carrying a fresh-payload anchor proving the page still reveals and retracts the cookie as it does, and the feed pair additionally requiring that a refused visit logs no `settings.calendar_feed_reveal` egress event; the class-wide, no-allowlist sweep `TestEverySealedCookiePayloadCarriesAServerVerifiedExpiry` in [internal/api/sealed_payload_expiry_sweep_test.go](internal/api/sealed_payload_expiry_sweep_test.go), which derives the roster from the declared cookie specs and cross-checks it against every cookie-name constant the package declares, then mints each one, rewrites the bound its payload carries into the past, re-seals it, and requires the production reader to refuse it |
| A shown-once reveal is made single use by a **server-side consumption mark**, not by the cleared cookie in the reveal response: `users.recovery_code_revealed_at` and `users.calendar_feed_revealed_at` (migration 036) are claimed with a compare-and-set before anything is rendered, so replaying the original sealed value on the owner's own session is refused on every reveal surface — the dedicated recovery-code page, the inline post-registration block, and the calendar-feed reveal page. Every UPDATE that mints a fresh secret NULLs the matching mark in the same statement, so re-issuing arms exactly one new reveal and the mark never retires the surface. A refusal retracts the cookie in that same response and costs the display only: the account keeps its session and lands where it lands when no cookie is presented | `TestRecoveryCodeRevealRefusesAReplayedCookieAndRearmsOnRegenerate`, `TestRecoveryCodeRevealRefusesAReplayedInlineCookie` in [internal/api/auth_recovery_code_route_regressions_test.go](internal/api/auth_recovery_code_route_regressions_test.go), `TestCalendarFeedRevealRefusesAReplayedCookieAndRearmsOnRotate` in [internal/api/settings_calendar_feed_regressions_test.go](internal/api/settings_calendar_feed_regressions_test.go); the compare-and-set and the re-arm-on-mint in `TestClaimRecoveryCodeRevealIsSingleUsePerMint`, `TestClaimCalendarFeedRevealIsSingleUsePerMint`, `TestRevealClaimsAreScopedToTheirOwner`, and the no-allowlist mint sweep `TestEveryRecoveryCodeMintClearsItsRevealMark` in [internal/db/user_repository_reveal_mark_test.go](internal/db/user_repository_reveal_mark_test.go); the no-allowlist reveal-site sweep `TestEverySecretRevealClaimsItsConsumptionMark` in [internal/api/secret_reveal_consumption_sweep_test.go](internal/api/secret_reveal_consumption_sweep_test.go) |
| The two TOTP cookies (`ovumcy_totp_pending`, `ovumcy_totp_setup`) are session-scoped, so a value the server refuses is retracted in the same response: an unopenable envelope, a tampered value, a payload that is not the expected JSON, one naming no account, one carrying no secret, one minted for another account, and an expired one are each cleared — while a refusal about the submitted code leaves the cookie in place so the flow can be retried | `TestTOTPPendingCookieUnusableValueIsClearedNotLeftRiding`, `TestTOTPSetupCookieUnusableValueIsClearedNotLeftRiding` in [internal/api/totp_cookies_test.go](internal/api/totp_cookies_test.go); at the handler boundaries — challenge POST, challenge page, enrollment confirmation — `TestTOTPChallengeClearsAPendingCookieItCannotUse` in [internal/api/handlers_auth_2fa_test.go](internal/api/handlers_auth_2fa_test.go) and `TestVerifyTOTP2FAEnrollmentClearsASetupCookieItCannotUse` in [internal/api/handlers_settings_2fa_test.go](internal/api/handlers_settings_2fa_test.go) |
| OIDC sign-in cookies (`ovumcy_oidc_auth`, `ovumcy_oidc_stepup`) are `SameSite=None` (the provider posts the callback cross-site) and are therefore never issued or cleared without `Secure`, even where `COOKIE_SECURE` is off — while a same-site spec still follows `COOKIE_SECURE` | `oidc_state_cookie_test.go`, `oidc_stepup_cookie_test.go` in `internal/api/`, and all three legs of the guarantee — the declared `forceSecure`/`SameSite` on each spec, and the attribute actually written and cleared — in `TestSealedCookieCrossSiteSpecsDeclareForceSecure`, `TestWriteSealedCookieForcesSecureForCrossSiteSpecs`, `TestClearSealedCookieForcesSecureForCrossSiteSpecs` ([internal/api/sealed_cookie_force_secure_test.go](internal/api/sealed_cookie_force_secure_test.go)) |
| A request that outlives the 60-second request budget answers `503 request_timeout` with the handler's response discarded in full — headers included — so a session cookie or a redirect a handler had already committed is never delivered under a status that tells the caller nothing happened. The rollback is positional, not a list of header names: what the app writes before the guard (browser-hardening headers, the CSRF cookie) survives, what the handler wrote does not | `TestRequestDeadlineGuardDiscardsTheHandlerResponseOnAnExpiredBudget` in [internal/api/middleware_request_deadline_test.go](internal/api/middleware_request_deadline_test.go) |
| Login issues a sealed `ovumcy_auth` cookie (not a legacy JWT) | `TestLoginSetsSealedAuthCookieValue`, `TestAuthMiddlewareRejectsLegacyJWTAuthCookieFallback` in [internal/api/auth_cookie_compat_regression_test.go](internal/api/auth_cookie_compat_regression_test.go) |
| Remember-me toggles cookie persistence | `TestLoginRememberMeControlsCookiePersistence` in [internal/api/auth_login_remember_me_regressions_test.go](internal/api/auth_login_remember_me_regressions_test.go) |
| Logout retracts every sealed cookie scoped to the ended session — `ovumcy_auth`, the provider-logout bridge, `ovumcy_recovery_code`, `ovumcy_reset_password`, `ovumcy_totp_pending`, `ovumcy_totp_setup` and `ovumcy_calendar_feed` — rather than leaving one to expire on its own. Both TOTP cookies are session-scoped, so an abandoned enrollment would otherwise keep sending its sealed raw secret at `path: "/"` for the rest of the browser session, and the feed subscribe URL is a capability that sign-out does not revoke. Cookies consumed by a flow that completes outside a session (OIDC one-time state and step-up, register pickup) are deliberately untouched | `TestAuthLogoutPostWithCSRFRedirectsAndClearsCookies` in [internal/api/auth_logout_csrf_regression_test.go](internal/api/auth_logout_csrf_regression_test.go) |
| A session that ends on purpose or is torn down by the server — sign-out, account deletion (both the password-gated route and the OIDC step-up), the OIDC callback's rollback of a session it had just issued, and an abandoned register pickup — also retracts the two plaintext preference caches `ovumcy_lang`, the one-year cache of the account's interface language, and `ovumcy_tz`, the browser's own timezone — so a shared browser does not disclose that the app is used here, in which language, or from which region. `ovumcy_tz` stays retracted because the client volunteers the timezone only on a page rendered for a session: the bootstrap script is served only then, and the `X-Ovumcy-Timezone` header the middleware re-issues the cookie from rides only on those requests. A session the server merely REFUSES (expired or invalid cookie, unsupported role) deliberately keeps them: that path is reachable unauthenticated, and clearing there would cost a lapsed visitor the language of the page they are reading | `TestAuthLogoutPostWithCSRFRedirectsAndClearsCookies` in [internal/api/auth_logout_csrf_regression_test.go](internal/api/auth_logout_csrf_regression_test.go), `TestSettingsDeleteAccountDeletesUserAndClearsAuthRelatedCookies` in [internal/api/settings_delete_account_regression_test.go](internal/api/settings_delete_account_regression_test.go), `TestErasureStepupDeletesAccountOnlyAfterAFreshReauth` in [internal/api/settings_erasure_stepup_flow_test.go](internal/api/settings_erasure_stepup_flow_test.go), `TestOIDCCallbackTeardownRetractsTheLanguageCookieItJustIssued` and the boundary `TestSessionRejectionLeavesTheLanguageCookieInPlace` in [internal/api/account_language_regressions_test.go](internal/api/account_language_regressions_test.go); for the timezone half `TestLogoutRetractsTheTimezoneCookie` (both with and without the request header the middleware re-issues from) and its boundary `TestSessionRejectionLeavesTheTimezoneCookieInPlace` in [internal/api/timezone_cookie_retraction_test.go](internal/api/timezone_cookie_retraction_test.go), plus the client half in [web/src/js/__tests__/timezone-cookie-signed-in-only.test.mjs](web/src/js/__tests__/timezone-cookie-signed-in-only.test.mjs) |
| The public language switch (`POST /lang`) persists to `users.interface_language` only when the request carries a valid session whose account passes the same owner-role predicate `OwnerOnly` enforces. The stored value is gated on the same `IsSupportedLanguage` predicate `/interface` refuses on, so a language this build does not ship reaches neither the account nor the cookie as submitted — but the route's own answer is unchanged, because it must not depend on whether the caller holds a session: an unsupported code still falls back to the default cookie, and an unauthenticated switch stays cookie-only | `TestAuthenticatedLanguageSwitchPersistsToTheAccount`, `TestSwitchedLanguageSurvivesASessionReissue`, `TestUnauthenticatedLanguageSwitchStaysCookieOnly`, `TestAuthenticatedLanguageSwitchStoresNothingForAnUnsupportedLanguage`, `TestUnsupportedRoleLanguageSwitchStoresNothing`, `TestLanguageSwitchAnswersTheSameWithNoSessionAndWithADeadOne` in [internal/api/account_language_regressions_test.go](internal/api/account_language_regressions_test.go) |
| State-changing endpoints require a valid CSRF token | `state_mutation_csrf_regression_test.go`, `auth_logout_csrf_regression_test.go`, `settings_security_csrf_regression_test.go`, `export_csrf_regression_test.go`, `language_switch_csrf_regression_test.go` in `internal/api/` |
| The CSRF middleware exempts exactly ONE mutating route — `POST /auth/oidc/callback` (POST-bound); the query-mode `GET` callback is a safe method outside CSRF, protected by the same sealed one-time state — and every other mutating route in the real app refuses a token-less request with 403 | `TestCSRFExemptionListIsExactlyOneRoute`, `TestCSRFDeniesEveryMutatingRouteWithoutToken` in [cmd/ovumcy/csrf_exemption_guard_test.go](cmd/ovumcy/csrf_exemption_guard_test.go) |
| Beyond the token, the CSRF middleware matches the request `Origin` against the app-observed scheme+host on every mutating request: a foreign `Origin` is refused in any posture, a MISSING one is refused when the app is served over HTTPS (with `Referer` as the sanctioned fallback), and the app only observes HTTPS through a terminator it is configured to trust — so `TRUST_PROXY_ENABLED=false` behind a terminator turns every form POST into a 403 rather than weakening the check | `TestCSRFRefusesOriginlessMutationBehindHTTPSTerminator`, `TestCSRFRefusesForeignOriginMutation`, `TestCSRFAcceptsMatchingOriginMutationBehindHTTPSTerminator`, `TestCSRFAcceptsMatchingRefererWhenOriginAbsentBehindHTTPSTerminator`, `TestCSRFToleratesOriginlessMutationOnPlainHTTP` in [cmd/ovumcy/csrf_origin_regression_test.go](cmd/ovumcy/csrf_origin_regression_test.go) |
| `OIDC_RESPONSE_MODE=form_post` (default) pins `response_mode=form_post` on the authorize URL and reads the callback from the POST body; `query` omits the parameter, serves the callback over `GET`, and reads it from the URL query — a single source per mode, with the PKCE S256 challenge and nonce preserved in both. The PKCE verifier is never in transport (authorize URL nor callback), only in the sealed `HttpOnly` state cookie, so a query-mode code in the URL is inert | `TestAuthCodeURLResponseModeParam`, `TestOIDCVerifierNeverInTransport` in [internal/security/oidc_response_mode_test.go](internal/security/oidc_response_mode_test.go), `TestOIDCCallbackQueryMode*`, `TestOIDCCallbackFormPostMode*` in [internal/api/auth_oidc_response_mode_test.go](internal/api/auth_oidc_response_mode_test.go) |

### Retention and Deletion

| Claim | Enforced by |
| --- | --- |
| `clear-data` deletes daily_logs and user-defined symptoms and resets preferences | `settings_clear_data_flow_test.go` in `internal/api/` |
| `clear-data` does **not** touch email, password hash, recovery code hash, role, display name, OIDC identity links, TOTP state, onboarding status, or the interface language (`interface_language` — a display preference, not part of the health record) | `TestClearDataPreservesAccountIdentityFields` in [internal/api/settings_clear_data_preservation_test.go](internal/api/settings_clear_data_preservation_test.go) |
| `delete-account` deletes daily_logs, all symptoms, and the users row | `settings_delete_account_regression_test.go` in `internal/api/`, `TestOperatorUserService*` in [internal/services/operator_user_service_test.go](internal/services/operator_user_service_test.go) |
| `delete-account` erases `oidc_identities` **explicitly, not via the `ON DELETE CASCADE` FK**: FK enforcement is a runtime pragma, so an instance running with `foreign_keys` off would otherwise keep auth-linkage rows for an erased account. `register_pickup_tokens` has no FK at all, and `oidc_logout_states` rows predating migration 031 carried a NULL `user_id` until migration 033 deleted them — every remaining row is attributable and covered by the same explicit per-user delete. The cascade declared in `migrations/014_oidc_identities.sql` stays as defence in depth, never as the guarantee | `TestDeleteAccountAndRelatedDataRemovesAllUserRows` (every user-scoped table is empty afterwards) and `TestDeleteAccountAndRelatedDataRollsBackOnChildDeleteError` (drives the failure branch per child table, `oidc_identities` included) in [internal/db/delete_account_completeness_test.go](internal/db/delete_account_completeness_test.go) |
| Both danger-zone actions require the current password | `settings_clear_data_flow_test.go`, `settings_delete_account_regression_test.go` in `internal/api/` |
| An account with no local password confirms both danger-zone actions with a fresh OIDC step-up instead; an account that has a local password is refused that route, so it cannot stand in for the password gate | `TestErasureStepupRefusesAccountsThatHaveALocalPassword`, `TestErasureStepupStartDoesNotEraseAnything` in [internal/api/settings_erasure_stepup_regression_test.go](internal/api/settings_erasure_stepup_regression_test.go) |

### Password & Auth Policy

| Claim | Enforced by |
| --- | --- |
| Passwords require ≥ 8 Unicode code points | `TestValidatePasswordStrength_RejectsWeakPasswords` (`"Short1"` case) in [internal/services/password_policy_test.go](internal/services/password_policy_test.go) |
| Passwords longer than 72 bytes are rejected at validation (bcrypt input limit) | `TestValidatePasswordStrength_EnforcesBcryptByteLimit` in [internal/services/password_policy_test.go](internal/services/password_policy_test.go) |
| Passwords require at least one uppercase, one lowercase, and one digit | `TestValidatePasswordStrength_RejectsWeakPasswords` (alllowercase / ALLUPPERCASE / NoDigitsHere cases) in [internal/services/password_policy_test.go](internal/services/password_policy_test.go) |
| Strong password passes | `TestValidatePasswordStrength_AcceptsStrongPassword` in [internal/services/password_policy_test.go](internal/services/password_policy_test.go) |
| Change-password rejects weak password and mismatch | `TestChangePasswordRejectsWeakNumericPassword`, `TestChangePasswordRejectsPasswordMismatch` in [internal/api/auth_change_password_validation_test.go](internal/api/auth_change_password_validation_test.go) |
| Recovery code is bcrypt-hashed at rest | `TestGenerateRecoveryCodeHash` in [internal/services/auth_reset_policy_test.go](internal/services/auth_reset_policy_test.go) |
| New password and recovery-code hashes are stamped at cost 12 (`passwordHashCost`, above `bcrypt.DefaultCost`) | `TestNewPasswordHashesUseConfiguredCost`, `TestPasswordHashCostIsAboveDefault` in [internal/services/auth_service_hash_cost_test.go](internal/services/auth_service_hash_cost_test.go) |
| TOTP secret is encrypted under a key derived from `SECRET_KEY`; stored value not equal to plaintext | `internal/security/field_crypto_test.go` (see Field-Level Encryption above) |
| TOTP code reuse within the same 30-second step is rejected | `totp_service_test.go`, `user_repository_totp_step_test.go`, `handlers_auth_2fa_test.go` |
| TOTP enrollment rejects 6-digit codes that do not match | `handlers_settings_2fa_test.go` |
| The TOTP enrollment seed is shown in the page HTML on `GET /settings/2fa` — the second declared exception to *no usable secret in transport*, and inert until confirmed: it is minted fresh on each visit and never re-served from storage (an account that already has 2FA on gets the management view instead, with no QR and no key generated), it lives only in the sealed owner-bound `ovumcy_totp_setup` cookie until the first code verifies, and a refused code enables nothing and persists nothing. Once that code verifies and `EnableTOTP` has committed the enrollment, the seed is spent and the cookie is retracted on **every** exit the confirmation can take — including the refusal raised when this device's session cannot be re-issued after the `auth_session_version` bump | `TestShowTOTPSetupPage_TOTPNotEnabled_RendersQRAndSecret`, `TestShowTOTPSetupPage_TOTPEnabled_ShowsManagementView`, `TestVerifyTOTP2FAEnrollment_InvalidCode_DoesNotEnable`, `TestVerifyTOTP2FAEnrollmentClearsTheSetupCookieWhenTheSessionReissueIsRefused` in [internal/api/handlers_settings_2fa_test.go](internal/api/handlers_settings_2fa_test.go) |
| Forgotten password reset rejects wrong recovery code | `TestAuthServiceFindUserByEmailRecoveryCodeAndPasswordRejectsMismatch`, `TestAuthServiceFindUserByEmailRecoveryCodeAndPasswordRejectsMissingUser` in [internal/services/auth_service_recovery_test.go](internal/services/auth_service_recovery_test.go) |
| `POST /api/v1/password-resets` requires the account's **current password alongside** the recovery code: the code substitutes for the second factor (TOTP), never for the first, so one captured secret can no longer mint a reset token, rewrite the password, and be handed a session. A submission with an empty or a wrong password mints no reset token and sets no `ovumcy_auth` cookie, and it consumes an attempt on both rate-limit buckets. A JSON body carrying no `password` member at all is the shape the endpoint accepted before v2.0.0, and is refused by its own key before any credential is read, so an integrator is not sent hunting a recovery code that is in fact correct | `TestRecoveryResetRefusesWithoutTheAccountPassword`, `TestRecoveryResetIssuesAResetTokenForPasswordAndRecoveryCode` in [internal/api/auth_recovery_reset_requires_password_regressions_test.go](internal/api/auth_recovery_reset_requires_password_regressions_test.go), `TestRecoveryResetNamesTheV1BodyItNoLongerAccepts` in [internal/api/api_v1_declared_breaks_contract_test.go](internal/api/api_v1_declared_breaks_contract_test.go), `TestAuthServiceFindUserByEmailRecoveryCodeAndPasswordRejectsWrongPassword` in [internal/services/auth_service_recovery_test.go](internal/services/auth_service_recovery_test.go), `TestPasswordResetServiceStartRecoveryWrongPasswordAddsFailure` in [internal/services/password_reset_service_test.go](internal/services/password_reset_service_test.go) |
| Adding the password operand adds no oracle: a wrong password, a wrong recovery code, an account with local auth disabled, and an unregistered address answer with the identical status and the identical body, and each spends the same two cost-12 bcrypt comparisons — the two credential compares are never short-circuited against each other, and the early-return equalizer spends both | `TestRecoveryResetKeepsWrongPasswordIndistinguishableFromWrongRecoveryCode` in [internal/api/auth_recovery_reset_requires_password_regressions_test.go](internal/api/auth_recovery_reset_requires_password_regressions_test.go), `TestRecoveryLookupSpendsBothCredentialComparesWithoutShortCircuit` in [internal/services/auth_service_recovery_lookup_timing_barrier_test.go](internal/services/auth_service_recovery_lookup_timing_barrier_test.go) |
| `POST /api/v1/password-resets/redeem` decides from the token's own SIGNED `Purpose` claim, never a cookie-carried bool: a **recovery** token and a **forced-from-LOCAL** token (plain login, and OIDC link-confirm's own password challenge) both refuse once local public auth is off — the factor that produced them is exactly what the operator disabled — while a **forced-from-OIDC** token (minted by the OIDC callback alone, without ever checking a local password) still redeems, since gating it would strand an account carrying `must_change_password` on an `oidc_only` instance. `GET /reset-password` follows the same gate. A token that fails to parse for its own reasons (expired, malformed, unrecognised/legacy purpose) is answered with the ordinary invalid-token response rather than the local-sign-in refusal, on both the redeem and the page | `TestRecoveryResetRedeemRefusesOnceLocalPublicAuthIsOff`, `TestForcedResetFromLocalLoginRedeemRefusedWhenLocalPublicAuthIsOff`, `TestForcedResetFromOIDCRedeemSurvivesLocalPublicAuthBeingOff`, `TestForcedResetFromOIDCExpiredTokenGetsInvalidTokenNotLocalAuthDisabled`, `TestOIDCLinkConfirmForcedResetMintsLocalPurposeRefusedWhenLocalPublicAuthIsOff`, `TestLegacyOrUnrecognisedPurposeResetTokenRedeemRefusedAsInvalid`, `TestResetPasswordPageFollowsTheRedeemGate` in [internal/api/auth_reset_local_auth_gate_regressions_test.go](internal/api/auth_reset_local_auth_gate_regressions_test.go) |
| Reset token rejects expired / wrong-purpose / state-mismatched inputs; the redeem-time Purpose check is an ALLOW-list of the three current values (recovery, forced-from-OIDC, forced-from-local), so an unrecognised or legacy Purpose refuses rather than being reinterpreted as one of them | `TestParsePasswordResetTokenRejectsExpired`, `TestParsePasswordResetTokenRejectsWrongPurpose`, `TestAuthServiceResolveUserByResetTokenRejectsStateMismatch` in [internal/services/auth_reset_policy_test.go](internal/services/auth_reset_policy_test.go), [internal/services/auth_service_recovery_test.go](internal/services/auth_service_recovery_test.go) |
| Auth session token rejects expired / wrong-signature / wrong-algorithm | `TestParseAuthSessionTokenRejectsExpired`, `TestParseAuthSessionTokenRejectsInvalidSignature`, `TestParseAuthSessionTokenRejectsWrongAlgorithm` in [internal/services/auth_session_policy_test.go](internal/services/auth_session_policy_test.go) |

### Rate Limits

| Claim | Enforced by |
| --- | --- |
| Per-account rate-limit keys are HMAC-derived from `SECRET_KEY` (no raw identity persisted) | `TestAuthAttemptPolicyKeysUseScopedHMACFingerprint`, `TestAuthAttemptPolicyKeysOmitIdentityFingerprintForBlankIdentity` in [internal/services/auth_attempt_policy_test.go](internal/services/auth_attempt_policy_test.go) |
| Attempt limiter respects window and reset semantics | `TestAttemptLimiterWindowAndReset`, `TestAttemptLimiterMultiKeyOperations` in [internal/services/attempt_limiter_test.go](internal/services/attempt_limiter_test.go) |
| Logout endpoint is rate-limited per account | `auth_logout_rate_limit_regression_test.go` in `internal/api/` |
| OIDC link-confirm password challenge shares the login failure budget (correct password refused once exhausted) — the route still serves, but this challenge sits past the pending-link cookie read, which succeeds for nobody since issue #701; see the note in *OIDC Account Linking* | `TestCompleteOIDCLinkConfirmationRateLimitsPasswordAttempts` in [internal/api/auth_oidc_regressions_test.go](internal/api/auth_oidc_regressions_test.go) |
| Attempt limiter memory is hard-capped under a fresh-key flood | `TestAttemptLimiterSizeCapUnderFreshKeyFlood`, `TestAttemptLimiterSizeCapEvictsColdestKeysFirst` in [internal/services/attempt_limiter_test.go](internal/services/attempt_limiter_test.go) |
| TOTP login rate-limited at 5 failures / 15 min | `TestVerifyTOTPLogin_RateLimited_HTMXReturns429` in `handlers_auth_2fa_test.go` |
| Password re-authentication on the erasure and password-change flows draws on a per-account budget (5 failures / 15 min); an exhausted budget refuses the correct password with `429`, so these cannot become faster password oracles than the login form | `TestVerifyReauthPasswordRefusesCorrectPasswordOnceBudgetSpent`, `TestVerifyReauthPasswordSuccessResetsBudget`, `TestVerifyReauthPasswordDoesNotSpendBudgetOnBlankSubmission` in [internal/services/settings_service_danger_test.go](internal/services/settings_service_danger_test.go); `TestValidateClearDataPasswordRateLimitsGuessesAndRefusesCorrectPassword` in [internal/api/settings_clear_data_flow_test.go](internal/api/settings_clear_data_flow_test.go) |
| The re-auth budget is keyed per account, so one owner exhausting it cannot lock out another owner sharing the same address (household self-hosting) | `TestVerifyReauthPasswordRefusesCorrectPasswordOnceBudgetSpent` in [internal/services/settings_service_danger_test.go](internal/services/settings_service_danger_test.go) |
| The `.ics` feed has its own per-IP budget, strictly tighter than the `/api` catch-all — the feed limiter must never inherit the cheap-read budget. It was sized when every well-formed request cost a bcrypt compare; migration 032 moved verification to a keyed MAC, so the budget now bounds a cookieless unauthenticated surface (and the residual bcrypt of any not-yet-migrated row) rather than ~79 CPU-seconds per minute | `TestLoadRuntimeConfigGivesCalendarFeedItsOwnBudget`, `TestCalendarFeedLimiterUsesItsOwnBudgetNotTheAPIBudget` in [cmd/ovumcy/main_test.go](cmd/ovumcy/main_test.go) and [cmd/ovumcy/rate_limit_negotiation_test.go](cmd/ovumcy/rate_limit_negotiation_test.go) |
| TOTP disable rate-limited at 5 failures / 15 min | `handlers_settings_2fa_test.go` |
| Rate-limit error response is sanitized and contains no PII | `TestAuthRateLimitHandlerLogsSecurityEventWithoutPII` in [cmd/ovumcy/main_test.go](cmd/ovumcy/main_test.go) |
| Every limiter answers through the shared error envelope (`error` plus `error_detail`) with its own stable key, carrying `retry_after_seconds` as an extension member bounded by the `Retry-After` header it echoes — so one refusal cannot reach a client in two shapes depending on which layer produced it | `TestEveryRateLimiterAnswersThroughTheSharedEnvelope` in [cmd/ovumcy/rate_limit_envelope_regression_test.go](cmd/ovumcy/rate_limit_envelope_regression_test.go) |
| A refused browser request never receives raw JSON: the auth and settings forms keep their flash redirect, and the language switch — the one public form with no HTMX behind it — renders the localized status fragment as `text/html` | `TestEveryRateLimiterAnswersABrowserWithoutRawJSON` in [cmd/ovumcy/rate_limit_envelope_regression_test.go](cmd/ovumcy/rate_limit_envelope_regression_test.go) |
| A rate-limited HTMX flow renders localized copy, not the machine key: the limiters sit ahead of the language middleware, so a refusal resolves the locale catalogue itself | `TestRateLimitedHTMXFlowsRenderLocalizedCopy` in [cmd/ovumcy/rate_limit_envelope_regression_test.go](cmd/ovumcy/rate_limit_envelope_regression_test.go) |
| Every limiter registered by the composition root is covered by the two sweeps above — the surface table is checked against the real registration count, so a limiter added later cannot be omitted | `TestRateLimitSurfaceTableCoversEveryLimiter` in [cmd/ovumcy/rate_limit_envelope_regression_test.go](cmd/ovumcy/rate_limit_envelope_regression_test.go) |
| Edge rate limiters key on the real client IP (rightmost untrusted `X-Forwarded-For` hop); a spoofed prefix shares one bucket, and an untrusted peer / disabled proxy ignores the header | `TestRateLimitKeyGeneratorBucketing`, `TestRightmostUntrustedIP`, `TestTrustedProxyMatcher` in [cmd/ovumcy/rate_limit_keygen_test.go](cmd/ovumcy/rate_limit_keygen_test.go) |
| An edge limiter scoped to one endpoint covers every spelling of that path the router accepts — case and trailing slashes are folded before the comparison, so a change of case cannot escape the endpoint's own budget — and no more: a path merely sharing its prefix still draws on a different budget | `TestScopedRateLimitersCoverEveryRoutableSpellingOfTheirPath`, `TestFiberRoutesEveryScopedLimiterPathSpellingToItsOwnRoute` in [cmd/ovumcy/rate_limit_scope_guard_test.go](cmd/ovumcy/rate_limit_scope_guard_test.go) |

### SECRET_KEY Usage Map

| Claim | Enforced by |
| --- | --- |
| Sealed cookies derive their key from `SECRET_KEY` via HKDF with versioned salt/info labels | `secure_cookie_codec_rotation_test.go`, `secure_cookie_codec_security_test.go` in `internal/api/` |
| Field encryption uses a distinct HKDF label set; AAD prevents cross-row swap | `TestDecryptField_RejectsWrongAAD` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Cookie and field label sets derive distinct keys; a payload sealed for one purpose cannot be opened as the other | `TestSealedCipherPurposeKeySeparation` in [internal/security/sealed_cipher_test.go](internal/security/sealed_cipher_test.go) |
| Rate-limit identity HMAC uses a distinct domain-separation label | `TestAuthAttemptPolicyKeysUseScopedHMACFingerprint` in [internal/services/auth_attempt_policy_test.go](internal/services/auth_attempt_policy_test.go) |
| Rotating `SECRET_KEY` breaks field-encrypted TOTP secrets (DecryptField with the new key fails) | `TestDecryptField_WrongKey` in [internal/security/field_crypto_test.go](internal/security/field_crypto_test.go) |
| Rotating `SECRET_KEY` also breaks the second field-encrypted column, `users.webhook_url`. The daily reminder pass fails **safe** rather than delivering to a garbage target: the owner whose ciphertext no longer opens is skipped (owner id logged, never the URL), the remaining owners still receive their reminders, and the pass does not error | `TestNotifyDecryptFailureSkipsOwner` in [internal/services/webhook_notify_service_test.go](internal/services/webhook_notify_service_test.go) |
| The calendar-feed verifier MAC derives from its own HKDF label pair, so it is unrelated to the cookie and field-crypto keys, and the stored MAC is worthless without `SECRET_KEY` (a database leak alone still cannot verify guessed verifiers offline). Rotating the key therefore disarms armed feeds: a mismatching MAC is refused outright, never re-checked against the row's bcrypt hash | `TestCalendarFeedVerifierMACRejectsWrongInputs` (its `"rotated key"`, `"wrong selector"` and `"halves swapped"` cases) and `TestCalendarFeedVerifierMACRequiresKey` in [internal/security/calendar_feed_mac_test.go](internal/security/calendar_feed_mac_test.go), `TestVerifyCalendarFeedTokenPrefersMACOverBcrypt` in [internal/services/calendar_feed_token_test.go](internal/services/calendar_feed_token_test.go), `TestResolveFeedRefusesStaleMACWithoutBcryptFallback` in [internal/services/calendar_feed_service_test.go](internal/services/calendar_feed_service_test.go) |
| The calendar-feed **key epoch** derives from its own HKDF label pair and additionally binds the feed-MAC label pair, so either a key rotation or a MAC label bump changes it; the boot sentinel then disarms the pre-032 rows whose bcrypt hashes a rotation cannot reach. The golden vector pins all four labels — changing any of them is a deliberate, release-noted event | `TestCalendarFeedKeyEpochGoldenPinsLabelSet`, `TestCalendarFeedKeyEpochDeterministicAndKeySeparated`, `TestCalendarFeedKeyEpochRequiresSecretKey` in [internal/security/calendar_feed_key_epoch_test.go](internal/security/calendar_feed_key_epoch_test.go) |

### Threat Model

| Claim | Enforced by |
| --- | --- |
| OIDC `end_session_endpoint` is host-pinned; cross-origin endpoint is dropped, logout falls back to local | `TestOIDC_RuntimePoC_HostPinRejectsCrossOriginEndSessionEndpoint`, `TestOIDC_RuntimePoC_HostPinAcceptsSameOriginEndSessionEndpoint` in [internal/security/oidc_runtime_poc_test.go](internal/security/oidc_runtime_poc_test.go) |
| OIDC `jwks_uri` is origin-pinned to the issuer; a cross-origin `jwks_uri` from discovery is rejected before any verifier is built | `TestOIDC_RuntimePoC_JWKSOriginPinRejectsCrossOrigin`, `TestOIDC_RuntimePoC_JWKSOriginPinAcceptsSameOrigin` in [internal/security/oidc_runtime_poc_test.go](internal/security/oidc_runtime_poc_test.go); `TestValidateDiscoveredJWKSURI` in [internal/security/oidc_test.go](internal/security/oidc_test.go) |
| OIDC `token_endpoint` is origin-pinned to the issuer; a cross-origin or non-https endpoint from discovery is rejected before any code exchange | `TestValidateDiscoveredTokenEndpoint` in [internal/security/oidc_test.go](internal/security/oidc_test.go) |
| OIDC HTTP client refuses redirects that leave the issuer origin (discovery/JWKS/token requests cannot be steered off-origin by a redirecting response) | `TestOIDC_RuntimePoC_DiscoveryRedirectCrossOriginRefused`, `TestOIDC_RuntimePoC_DiscoveryRedirectSameOriginFollowed` in [internal/security/oidc_runtime_poc_test.go](internal/security/oidc_runtime_poc_test.go); `TestOIDCRedirectPolicyPinsIssuerOrigin` in [internal/security/oidc_test.go](internal/security/oidc_test.go) |
| OIDC ID-token signing-algorithm allowlist rejects symmetric algorithms and `none` | `TestOIDC_RuntimePoC_AlgorithmConfusionRejected`, `TestOIDC_RuntimePoC_AlgorithmNoneRejected` in [internal/security/oidc_runtime_poc_test.go](internal/security/oidc_runtime_poc_test.go) |
| OIDC step-up reauth requires a matching `(issuer, subject)` identity for the current user | `auth_oidc_v2_regressions_test.go`, `settings_oidc_local_password_setup_test.go` in `internal/api/` |
| OIDC first-time link to a pre-existing local-auth account is refused without password confirmation (returns `ErrOIDCLinkRequiresConfirmation`; no identity row is created in the bypass path) | `TestOIDCLoginServiceAuthenticateRequiresConfirmationOnFirstLinkToExistingEmail` in [internal/services/oidc_login_service_test.go](internal/services/oidc_login_service_test.go) |
| OIDC link confirmation persists the identity link after a successful password confirmation | `TestOIDCLoginServiceConfirmAndLinkIdentityPersistsLink` in [internal/services/oidc_login_service_test.go](internal/services/oidc_login_service_test.go) |
| OIDC link confirmation refuses if the `(issuer, subject)` was concurrently claimed by a different user | `TestOIDCLoginServiceConfirmAndLinkIdentityRefusesCrossUserClaim` in [internal/services/oidc_login_service_test.go](internal/services/oidc_login_service_test.go) |
| HTMX error fragments are DOM-built, not assigned via `innerHTML` (defense-in-depth XSS) | `web/src/js/__tests__/` JS unit suite (`npm run test:unit`) |
| All other threat-model entries (operator-as-adversary, endpoint compromise, side-channel) are **policy-level** — Ovumcy explicitly declares these out of scope | Not test-enforceable |

### Logging Policy

| Claim | Enforced by |
| --- | --- |
| `AUDIT_LOG_ENABLED=false` (default) emits no `security event:` lines | `TestAuditLogDefaultOffSuppressesSecurityEvents` in [internal/api/security_event_logging_audit_flag_test.go](internal/api/security_event_logging_audit_flag_test.go) |
| `AUDIT_LOG_ENABLED=true` emits security-event lines | `TestAuditLogEnabledRestoresSecurityEvents` in [internal/api/security_event_logging_audit_flag_test.go](internal/api/security_event_logging_audit_flag_test.go) |
| When enabled, day-write logs the sanitized path `/api/v1/days/:date` (no concrete date) | `TestUpsertDayLogsSanitizedPathWithoutConcreteDate` in [internal/api/security_event_logging_mutation_regression_test.go](internal/api/security_event_logging_mutation_regression_test.go) |
| When enabled, symptom mutation log does not leak the user-supplied symptom name | `TestCreateSymptomLogsMutationWithoutLeakingUserInput` in [internal/api/security_event_logging_mutation_regression_test.go](internal/api/security_event_logging_mutation_regression_test.go) |
| Every state-changing endpoint that persists a settings column is audited: the reminder window and the timezone as `domain="health_data"` (they decide when a prediction is announced and which day it lands on), the display name as `domain="account"` — asserted to stay out of the health-data filter — and none of the three echoes the submitted value | `TestSettingsMutationsWithPersistenceAreAudited` in [internal/api/security_event_logging_mutation_regression_test.go](internal/api/security_event_logging_mutation_regression_test.go) |
| Onboarding is audited under the same domain and target as the settings cycle form it duplicates, so `domain="health_data" target="cycle_settings"` selects a cycle-baseline change whichever surface produced it; completion carries `target="day_entry"` for the period days it seeds | `TestOnboardingStepsAreAuditedLikeTheSettingsCycleForm` in [internal/api/security_event_logging_mutation_regression_test.go](internal/api/security_event_logging_mutation_regression_test.go) |
| Every export outcome — served, refused, failed — is tagged `domain="health_egress"` with a `target` and the `export_format` it was asked for, including the refusals raised before any data is read | `TestExportIsAuditedAsHealthEgressOnEveryOutcome`, `TestExportPrologueAttributesTheFormatWithoutASession` in [internal/api/security_event_logging_egress_regressions_test.go](internal/api/security_event_logging_egress_regressions_test.go) |
| Revealing the `.ics` subscribe URL or a recovery code is audited as egress and records the fact only — never the URL, the feed token, or the code — on every surface that displays one | `TestCalendarFeedRevealIsAuditedAsHealthEgress`, `TestRecoveryCodeRevealIsAuditedOnEverySurfaceThatShowsIt` in [internal/api/security_event_logging_egress_regressions_test.go](internal/api/security_event_logging_egress_regressions_test.go) |
| The egress and mutation domains stay disjoint, so `domain="health_data"` keeps selecting only the actions that changed or destroyed data | `TestHealthAuditDomainsSeparateEgressFromMutation` in [internal/api/security_event_logging_egress_regressions_test.go](internal/api/security_event_logging_egress_regressions_test.go) |
| A JSON restore is tagged `domain="health_data"` with a `target` and carries its added/skipped/rejected counts | `TestImportJSONSuccessIsAuditedAsHealthDataMutation` in [internal/api/imports_regressions_test.go](internal/api/imports_regressions_test.go) |
| CSRF middleware error path does not leak PII into the audit log | `TestCSRFMiddlewareErrorHandlerLogsSecurityEventWithoutPII` in [cmd/ovumcy/main_test.go](cmd/ovumcy/main_test.go) |
| Rate-limit handler does not leak PII into the audit log | `TestAuthRateLimitHandlerLogsSecurityEventWithoutPII` in [cmd/ovumcy/main_test.go](cmd/ovumcy/main_test.go) |
| The Fiber request log's error field is sanitized (emails → `:email`, opaque tokens → `:token`) | `TestSafeLogError` in [internal/api/request_logging_test.go](internal/api/request_logging_test.go) |
| The same sanitization also covers the short secrets that fall below the opaque-token length floor — a recovery code (`OVUM-XXXX-XXXX-XXXX`) and a submitted six-digit code both become `:code` — while an ISO date, an HTTP status, a route template and a longer numeric id stay readable | `TestSafeLogErrorMasksSecretShapesAndKeepsDiagnostics` in [internal/api/request_logging_test.go](internal/api/request_logging_test.go) |

### Medical Safety Disclaimer

The backend HTML contract normally forbids asserting localized copy, but the persistent medical-safety disclaimer is a deliberate exception: its exact wording is the invariant, so these tests pin both the surface's stable `data-*` hook and the safety string. Withholding is the other half of that invariant — a disclaimer does not license a window the recorded data cannot support — so the check that keeps the decision to withhold in one place is listed here too.

| Claim | Enforced by |
| --- | --- |
| Every owner-facing prediction surface (dashboard, stats, calendar) renders the persistent "estimates, not medical advice or a method of contraception" disclaimer, so a template refactor cannot silently drop it from a health-prediction page | `TestDashboardRendersPredictionDisclaimer`, `TestStatsRendersPredictionDisclaimer`, `TestCalendarRendersPredictionDisclaimer` in [internal/api/dashboard_prediction_disclaimer_test.go](internal/api/dashboard_prediction_disclaimer_test.go) |
| The two settings sections that ship predicted dates off the instance (webhook reminders, calendar feed) render the same disclaimer, so single-sourcing the wording into one catalogue key cannot reduce the number of surfaces that show it | `TestSettingsEgressSurfacesRenderPredictionDisclaimer` in [internal/api/dashboard_prediction_disclaimer_test.go](internal/api/dashboard_prediction_disclaimer_test.go) |
| No surface rebuilds a suppression gate out of its signals: withholding comes from `PredictionsSuppressed` / `FertilityProjectionSuppressed`, or from the decision the cycle context already resolved, so a signal added to the predicate is added once for every surface that asks it, instead of being written out per surface and missed at one. The sweep reads one Go expression at a time, so a gate assembled across intermediate variables, or in a template, is outside what it can see | `TestNoSurfaceRecombinesTheSuppressionSignals` finds the hand-built gates; `TestTheSweepKnowsEverySignalThePredicatesDisjoin` keeps that sweep's signal list answerable to the predicates, so a newly added signal cannot be read as an ordinary operand — both in [internal/services/prediction_suppression_recombination_barrier_test.go](internal/services/prediction_suppression_recombination_barrier_test.go) |
| Every surface that publishes an owner's projection — the stats page, the dashboard, the JSON API, the webhook reminder pass, the calendar feed — publishes through one adapter (`PublishedStats`), which clears the dates its verdict refuses and returns that verdict, so a surface cannot carry data the gate beside it has withheld. A signal added to a predicate must also gain a published reason, or a payload would withhold dates while naming no cause | `TestPublishedStatsClearsEveryDateItsVerdictRefuses` and `TestEverySuppressionSignalHasAPublishedReason` in [internal/services/prediction_published_stats_test.go](internal/services/prediction_published_stats_test.go) |
| `GET /api/v1/stats/overview` publishes an explicit DTO rather than the domain statistics struct, and withholds exactly what the pages withhold: projected dates are `null` under every suppression state, with a machine-readable reason and the safety disclaimer beside them | `TestStatsOverviewWithholdsEveryProjectionItsGatesRefuse` over the four states, `TestStatsOverviewPublishesAWholeProjectionWhenNothingSuppressesIt` for the unsuppressed tier, and `TestStatsOverviewAgreesWithTheStatsPageOnFertility` for API↔HTML parity — all in [internal/api/stats_overview_contract_test.go](internal/api/stats_overview_contract_test.go) |
